# The paragraph

A paragraph whose author wrapped it at whatever column his window happened to be,
and whose reader now has a window of a width the author never saw. The lines join
with one space — which is what CommonMark renders a soft line break as, so the
rendered page does not move.

A second paragraph, one line already, and it stays one line.
