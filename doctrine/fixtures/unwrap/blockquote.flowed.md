# The blockquote

> **Coda:** a quote's continuation lines join with the inner marker dropped, and the first line's marker is the one that survives the join.
>
> A second quoted paragraph, wrapped across two lines.

Plain prose after the quote, wrapped across two lines of its own.
