# The city — a durable distillation

`docs/` is where a distillation lives once it outgrows the master doc (DOCTRINE §3), so it is a
law surface and the speech arm reads it.

The rail runs down the outer edge and carries a command's binding on every icon.

The fold produces a turn's tally, which is the value the reduce yields.

A desk works a live batch; the paint is D13's word for the act of drawing, and a receipt is the
file or the wire, never the paint.

The step's ceiling is WALL_MS, and the constant names what it limits.

The glass is gone.
