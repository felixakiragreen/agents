A registered building whose founding has not run: the register declares it, the walk finds
no artifact, and that is a warning, never a failure. This file exists so git keeps the
directory — it is not a .md, so the walk still finds nothing here.
