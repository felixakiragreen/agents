# G3 — the tender's first batch, reviewed

**Status:** LANDED 2026-09-02 — three landings verified and kept, three rulings ruled and
cited, two grants landed, the prune done: lint names only belvedere's nineteen; findings
F1–F11 below. Laid 2026-09-01; the bar made attainable 2026-09-02 (grand-architect-23) ·
**Depends on:** 041; 042; 043 · **Staffing:** Architect · fable-high

## Mission

Review the tender's first run (D83): 041 · 042 · 043 verified against their bars, each
landing kept or reverted with a dated note; the board pruned to the retention law's cap;
the three rulings the findings hand this gate ruled and written on their rows; findings
distilled; the baton to ⬡.

## Inputs — read before working

- [BOARD.md](../BOARD.md) — the batch note and the three rows; the ledger tail from
  2026-09-01 on.
- The findings: [041](041-statement-caps.md) F1–F10 · [042](042-grid-prune.md) F1–F10 ·
  [043](043-citation-respell.md) F1–F12 and its hand list.
- The retention law — DOCTRINE §3 (D78): a resolved row compresses to status + findings
  pointer; the story stays in the charge doc.

## The bar

- **The prune.** `BOARD.md`'s twelve over-cap LANDED cells and `plans/018-great-recut.md`'s
  two compress to status + findings pointer. `doctrine lint ~/code/agents` then names only
  belvedere's nineteen (041-F8) — a retired building, its books built to die (D84); that red
  stands, written on this row, until the belvedere purge (the deferred list). Lint 0 is not
  this gate's bar; lint naming nothing but belvedere is.
- **Three rulings, the Architect's — reversible and cited, no word of his owed:**
  - **042-F4 — `haiku-low`.** The census rule says it dies (zero fires, probe mentions
    only); the spec's partition presumed it lived. Recommendation: **kill** — `git rm`, the
    README's history line gains its ninth name. The grid law is mint-at-the-lay, so a charge
    that names it re-mints it for free; "the last Haiku" is a roster shape nobody uses.
  - **041-F3 — BLOCKED is off the statement.** BLOCKED is transient and owes its review
    (DOCTRINE §4). Recommendation: it joins `LIVE_STATES` (`doctrine/src/credit.ts`).
  - **041-F10 — `--live` drops the register.** D82 makes the register the surface a credit
    mark sits on until he reads it. Recommendation: `liveFails` keeps `decisions`.
- **Two grants:** the one-line fixes 041-F3 and 041-F10 name are this gate's to land — each
  its own commit, `bun test doctrine` green, the evidence filed here. Nothing else is built
  at this desk.
- **Distill:** what the landings hand the durable docs goes there — 042's mint-at-the-lay
  is already in the README; 043's four rules land where DOCTRINE §8's sentence points.
  The rest stays in findings.

## Out of scope

Belvedere's cells and entries — the purge is a delete, a red act, his ⬡✓, its own charge
when stigmergon's salvage is done · the linter's discovery (the deferred list) · any new
law.

## Findings

Every command below was run at this desk on 2026-09-02, at `514538f` (043's landing) unless a
later commit is named.

**F1 — 041 verified against its bar: KEPT.** `bun test` in `doctrine/` 121 pass / 0 fail (109
at 041's landing; 043's twelve on top). `doctrine statement .` → `nothing on credit: no ⬡ go on a
live surface under ..` — the empty statement the bar names. `lint ~/code/agents` 33
`board.cell-cap` · 168 `ledger.entry-cap`, one failure class — the twelve agents cells and 018's
two named by row, byte for byte 041's list. 041-F2's measure (characters, not bytes) accepted:
the cap taxes what a reader reads. 041-F3 and 041-F10 are this gate's grants — F5, F6.

**F2 — 042 verified against its bar: KEPT.** `ls canon/agents/` twelve, and `ls
~/.claude/agents/ ~/.claude-thg-fgreen/agents/ ~/.claude-thg-doorbell/agents/` the same twelve
×3; `./sync/check` `green — 3×2 links, all pointing at canon.`; the dead-tier grep over `canon/`
returns the README's one history line (three physical lines) and nothing else; repo-wide `bun
test` **990 pass / 0 fail** across 57 files (977 at 042, plus 043's twelve and this gate's one).
042-F8 stands as read: the dispatched half of the bar's instrument does not exist, the fresh
session was the right substitute. 042-F4 is ruled at F4.

**F3 — 043 verified against its amended bar: KEPT.** `bun doctrine/cli.ts citations .` → `the
table's own, unconsumed — each a HAND edit: 0`; the 41 outside it are D78–D85 (live) and the
template's `**D1**` (a form shown); `Dry run: 0 edit(s) across 0 file(s)` — the fixed point holds
at HEAD. Suite 109 → 121 with the fixture; lint at 043's landing reads what a `514538f`-side
worktree reads (33/168) — the "unchanged" reading 042 set and 043-F9 asked for. The eleven
foreign ids inside the fence read `‹Name›:D‹n›` (043-F14's count, not the office's fourteen —
arithmetic, accepted). The hand list's 42 rows rode one commit (`cea0648`) before the
converter's (`9b1c3d1`), as the spec ordered.

**F4 — ruling 042-F4: `haiku-low` dies.** Citation: 042's blessed rule (*a tier file exists iff
the census names it in a Staffing cell or a rig fire*, probe mentions disqualified — Felix
2026-09-01, "let's kill them") and this gate's pre-chew; reversible — git holds the file. Its
five mentions are 012's probe evidence and 042's own table; zero fires. `git rm` at `9d765df`;
the README's history line names the ninth; `ls canon/agents/` **11**, `~/.claude*/agents/` 11
×3, `sync/check` green. Consequence, named: no Haiku tier is minted today. The README's rule of
thumb ("Haiku for drudgework") stays true as namespace guidance — mint-at-the-lay re-mints
`haiku-‹effort›` for free the day a Staffing cell names one. `TIERS` untouched, so 012's board
still parses.

**F5 — grant 041-F3 landed: BLOCKED is a live state for the statement.** `LIVE_STATES` in
`doctrine/src/credit.ts` gains `'BLOCKED'` (`adf9c5e`). Citation: DOCTRINE §4 — BLOCKED is
transient and *sits at the Architect's desk until it is replaced, killed, or escalated*, so a
mark on it still owes its review; D82's list named the three states and never chose against the
fourth. Pinned: the credit fixture's row 008 (`BLOCKED 2026-09-02`, its gate paid `⬡ go
2026-09-02`) appears on the statement at interest 0 — the fixture's known statement is now five,
`5 on credit · max interest 2`. Suite 121 → 121 (expectations widened, no test added).

**F6 — grant 041-F10 landed: `--live` keeps the decision register.** `liveFails` in
`doctrine/src/lint.ts` gains `|| f.artifact === 'decisions'` (`7998e14`). Citation: D82 — the
register is the surface a credit mark sits on *until he reads it*; `--live`'s 025 definition
predates the mark. **Red then green:** the new test — `credit-defects/DECISIONS.md` carrying an
undated `⬡ go`, asserting the live filter keeps a `credit.undated` whose artifact is
`decisions` — failed before the clause (`Expected − 1 / Received + 0`: the register's failure was
dropped) and passed after. Suite 121 → **122**. `lint ~/code/agents --live` reads the same 33
cells as the full lint, and 1 warning (the tail).

**F7 — the prune (`9186f1c`): lint 33 → 19, every survivor belvedere's.** Twelve `BOARD.md`
cells (012 · 014 · 015 · 022 · 029 · 030 · 032 · 036 · G2 · 037 · 038 · 040) and
`plans/018-great-recut.md`'s two (18g · 18h) compress to status + findings pointer. **Three
cells carried stories their charge docs lacked, and the story moved before the cell shrank**
(D78: the story stays in the charge doc): 014's smoke `⬡✓ 2026-08-29` — its doc still read *one
box open (Felix's smoke)*, the box is ticked and dated now; 036's and 037's *Reviewed and
verified 2026-08-31 (G2's Architect)* notes, which lived on the board alone, now close each
doc's Findings. **030's doc header read `OPEN — laid 2026-08-29` against a LANDED row** since
2026-08-29 — reconciled. The spent batch note died (D78; the tender holds its own copy in its
prompt). After: `19 failure(s) in 1 class(es) · 168 warning(s)` — `belvedere/README.md` 10,
`belvedere/v3/README.md` 9; `grep chars) | grep -vc belvedere` → **0**. The agents building's
own line reads `warn` (the entry-cap warnings; the ledger is D78-exempt on reads). Belvedere's
red stands on this row until the purge (the deferred list; 041-F8's question is the office's).

**F8 — distill: 043's four rules are already homed; nothing was added to canon.** Rule 1 (self
strips) and rule 2 (cross spells the home): DOCTRINE §8, the purge clause — *names the home …
and strips where it stands in that home; history keeps its numbers* (⬡✓ 2026-09-02). Rule 3
(a foreign id qualifies): STANDARD §7 — *bare at home, qualified abroad* (D80). Rule 4 (never
touched): §8's *history keeps its numbers* covers the tombstone and the board's records; a
ticked token is a form being named (040-F2, the vocabulary arm's mask since 023). The
instrument is documented at `doctrine/README.md` § `doctrine citations`. 042's mint-at-the-lay
is in the README (042-F6). **An amendment candidate, the office's, not this desk's** (no new law
here): 043-F15 — the next D78 kill records one `Home:` line **per clause**, not per entry; five
of the 39 rows legislated two homes and the converter cannot read which a sentence leans on. And
043-F15's two prose residues (`canon/mantles/README.md:7`, `:136`) wait for a prose sweep.

**F9 — the inbox: two entries, both canon-level, both the office's.** Filed 2026-09-02 by
stigmergon's 029 Architect: `Baton.shape` + `Baton.recommendation` as parser fields (the fifth
filing, a live consumer — grammar, D64's), and *sitting* vs the graveyard (the Standard's, a
successor or an exception line). The Architect never patches canon; both stand for the Grand
Architect's sweep and ride the queue behind the baton.

**F10 — D83's evidence, for the office's ruling on the `t` preset.** The tender's first run
ignited 041 → 042 → 043 → G3 on the note's schedule, paused once at a real fork (043 BLOCKED),
resumed on his word, and its writes stayed inside its access — the board's Status column and
the note's status line; every finding was the Builder's. The shape is proven in one run; whether
one run is enough is the deferred item's ruling.

**F11 — beyond the fence, standing.** `desk/stigmergon-settings.json` and
`desk/stigmergon-spaces.json` are dirty in the working tree with no hand on them (042-F10's
trap) — not touched, not committed, still tracked. 041's row keeps *lint red on purpose — 33
cells … the prune is G3's* as its record.

---

Kickoff (verbatim):

```
You are an Architect at fable-high.
Enter by the door — read ~/code/agents/canon/GUILD.md,
wear ~/code/agents/canon/mantles/architect.md,
then read ~/code/agents/plans/g3-tender-review.md and review the batch.
```
