# C25 — the respell sweep

**Status:** OPEN — laid 2026-08-29 · **Depends on:** C24 · **Staffing:** Architect · opus-high

## Mission

The city's live surfaces speak the standard — every register building: boards by
`doctrine migrate`, prose by the graveyard. The 18-continuation's form repairs are
absorbed here (same gesture, same buildings — two waves over one city would be waste).
History, voice, and the charters are fenced.

## Inputs — read before working

- `canon/work/STANDARD.md` — §9 the graveyard (the mapping) · §8 the language law ·
  §5 lore/law layers.
- C24's migrate dry-run counts (its findings) — the sizing.
- [plans/18-great-recut.md](18-great-recut.md) — the venue map and the re-run
  contract; [plans/19-doctrine-hardening.md](19-doctrine-hardening.md) §Findings —
  the entry tickets (whiteboardy, spacex, bob).
- 18f's findings (snappy's separator anatomy) — in [plans/18-great-recut.md](18-great-recut.md).
- D63 (molt clause — form free, meaning stone) · D32's meaning scope (still stands).

## The spec — blessed D71 ⬡✓ 2026-08-29; this map applies it

1. **Per building** — the register is read from the repo at execution time (formula
   18): `doctrine migrate --write` (boards: ⬡-gate, DEFERRED, unstaffed→`—`), diff
   reviewed; then the prose respell on live surfaces — master doc, CLAUDE.md, ISSUES
   header, OPEN/IN FLIGHT charge docs and their kickoffs. The graveyard is the
   mapping; quotes stay verbatim; historical ids are addresses, never renumbered.
2. **The fence — live vs not, explicit.** Live: boards whole, master docs, CLAUDE.mds,
   READMEs, open charge docs + kickoffs, inbox headers. Fenced: ledger entries,
   DECISIONS bodies, closed charge docs, findings sections (history — the respell is
   DEFERRED, Felix's word); `LOG.md`, `SAPHO.md`, every Personal Log (voice — lore
   keeps its windows); `dream.md` (immutable, D33); `canon/mantles/` (Felix's drafts
   gate the charters, C28); `canon/` generally (C23 did it).
3. **This repo:** MAP whole — §1–§10 prose, the §3 roster table reconciled to D71's
   ruling (the Dispatcher is dead; the flow engine, charge 20, is the shadow), §5's
   historical cells' machine tokens via migrate, batch-note prose by hand.
   `summon/presets.tsv`: the `d dispatcher` preset retired — the mantle is dead;
   `lab/08/run` derives its fixtures from the file (14's guard), re-run green.
   `docs/load-map.md`.
4. **The absorbed 18-continuation** — sanctions on record:
   - **whiteboardy re-fire:** migrate is clean post-19 (projected 104→2; the
     date-range head is a residue ruling).
   - **snappy separator repair:** sanctioned GA-11, form-only under the molt clause —
     the ~38 missing `---` restored; the re-framed entries take standard residue
     rulings.
   - **spacex decision heads:** 19's projection 7→0 ×2 — run and verify.
   - **bob's three inline ledger heads:** **sanctioned GA-13 (2026-08-29), same terms
     as snappy** — hoisted into `---`-separated entries, meaning byte-preserved.
5. **Escalation law:** a residue the graveyard doesn't map, or any edit that would
   touch meaning, stops that file — the residue is filed (the building's inbox, or
   this repo's), never guessed. A building that cannot land at quality lands what's
   done; the remainder becomes a new charge (doctrine law — pre-authorized here).
6. **Worktree law:** the cap-mega clusters ride their live branches per 18's venue
   map; nothing merges through this charge; commits per building in Felix's git style.
7. **Relay, not edit:** cap-mega's `--bless` golden flag (→ `--gild`, the standard §6)
   is that building's tooling — file the rename to its inbox with the standard cited;
   code flags belong to their own Architects. Belvedere's docs sweep here; its
   renderers and code are C27's (the batch is serial — no collision).

## Done when:

- `doctrine lint ~/code` → **0 across the register** — charge 18's inherited
  `Done when:`, now unblocked — evidence pasted.
- Dead-word grep (word-bounded, the graveyard column) → 0 on live surfaces per
  building, fences honored; the findings name every deliberately-skipped file.
- `lab/08/run` green after the preset retirement.
- Charge 18 reconciled **LANDED** on the MAP (its continuation lands here).
- Every residue ruled or filed — none guessed.

## Out of scope

Meaning — anything the molt clause reserves: supersedes, kills, status-truth changes.
History and voice (the fence). `canon/` (C23). The linter build (C26). Belvedere's
renderers (C27). The `--gild` code rename (relayed).

## Findings

*(append here)*

---

```
You are an Architect at opus-high.
Wear ~/code/agents/canon/mantles/architect.md,
then read ~/code/agents/canon/work/STANDARD.md (blessed law, D71)
and execute the charge at ~/code/agents/plans/c25-respell-sweep.md —
the city's live surfaces speak the standard.
```
