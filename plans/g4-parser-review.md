# G4 — the parser's second opening, reviewed

**Status:** LANDED 2026-09-09 → findings F1–F10; five kept, three rulings recorded, 044's law pasted (`bf3c04b`), 048's pasted 2026-09-09 by the office on its mark · **Depends on:** 044; 045; 046; 047; 048 · **Staffing:** Architect · fable-high · **The batch's doc** (stigmergon D40, adopted here 2026-09-08 at his word): the Mission below is the batch note; the landing record lands under Findings.

## Mission — the batch note

**The parser's second opening**, laid 2026-09-08 (grand-architect-24; his ⬡✓ on fork (b) in the room, 047 and 048 joined on his bless the same sitting): five members in order, then this gate. **044** (the boot pack — `doctrine boot ‹root›`; the ask) · **045** (the baton's fields — reads 044's baton line) · **046** (the lint gaps — no edge) · **047** (the unwrap — D88 built; runs late so it respells the batch's own docs once) · **048** (the ledger's aging — `doctrine prune`). **Serial, one checkout on `master`:** 044 → 045 → 046 → 047 → 048 → G4. `tender: sonnet-medium · plans/TENDER.md` — ignited by his hand on `plans/TENDER.md`'s fence plus one line naming this doc; the tender relays verbatim and authors nothing. **Parallel-safe: no** — every charge edits `doctrine/`, and 047 rewrites the corpus; one charge live at a time. **Concurrency plan:** none beyond serial — no live venue, no timed measurement.

**Red acts, pre-authorized:** the law's paste at this gate — 044's five amendments (⬡✓ 2026-09-08 on the text) and 048's four once its mark is recorded there; nothing else in the batch touches `canon/`, a charter, or the live wire (D88's line is live already). **Green's bar:** `bun test` green at HEAD and `doctrine lint ~/code/agents` naming only belvedere's nineteen. **Yellow, read at this gate:** the pack's layout inside 044's section order, 045's untyped words, 048's archive header — the Builder's calls inside the spec.

**The pauses:** the tender pauses at any escalation and at this gate's close — ⬡ verifies. 047 paused BLOCKED 2026-09-08 on two rulings and re-ignites 2026-09-09 on the desk's — F2a done by the office (`246851b`), F3's fix granted inside its fence (047 §Ruled at the desk); 048 follows it. A Builder whose bar cannot be met as written stops BLOCKED (047's word law and 048's guard interaction are the two places it might) — the Architect's desk, never the Builder's invention.

**This gate's review:** every landing verified against its own bar and kept or reverted with a dated note; the prune check (D78) — `doctrine prune` at its first use; the blessed law texts pasted into their homes under the grant below; three rulings pre-chewed here and written on their rows; findings distilled where this repo's docs take them; the baton to ⬡.

## Inputs — read before working

- [BOARD.md](../BOARD.md) — the batch note and the rows; the ledger tail from 2026-09-08 on.
- The charge docs and their Findings: [044](044-boot-pack.md) · [045](045-baton-fields.md) · [046](046-lint-gaps.md) · [047](047-unwrap.md) · [048](048-ledger-aging.md) — each `Done when:` with its pasted evidence.
- The retention law — DOCTRINE §3 (D78): a resolved row compresses to status + findings pointer; the story stays in the charge doc; a LANDED cell over 200 characters lints.
- The office's grant and the blessed texts — 044 and 048, each *The law at the landing*; D88 (the global file's line is live already — nothing to paste for 047).

## The bar

- **The suite and the lint, at HEAD:** `bun test` from `doctrine/` green; `doctrine lint ~/code/agents` names only belvedere's nineteen `board.cell-cap` (a retired building's books, D84 — that red stands until the belvedere purge on the deferred list); any new failure 046's arms raise on a live building is named on this row and stays red.
- **Each landing against its own bar** — the pasted evidence re-run, not re-read: the packs and their sizes (044), the census and the typed tail (045), the fixtures and their controls (046). A bar met by weakening a check is a rejection.
- **Three rulings, the Architect's — reversible and cited, no word of his owed:**
  - **(i) The pack's size.** If `doctrine boot` at a building exceeds 8 KB because its tail entry is long (`ledger.entry-cap` warns at 150 words; stigmergon's entries run past 400), the defect is the entry's, not the pack's: 044 LANDS with the finding, and the pack is never trimmed — the verbatim law (formula 9) outranks the size bar.
  - **(ii) A live board reddened by `board.gate-kickoff`.** The rows are named in the findings and stay red; the arm is never weakened and no building's doc is edited from this gate — its Architect owns the fix (a canon parser reads; it does not patch).
  - **(iii) A ⬡-action noun the type table does not know.** 045's census lists it; the Architect proposes the words for the office to add at its next summons, and adds none — the table is the standard's mirror, extended at the desk from data.
- **The grant — the law's paste.** 044's *The law at the landing* carries ⬡✓ 2026-09-08: paste its five amendments verbatim into DOCTRINE §11, `canon/mantles/README.md`, `canon/mantles/architect.md`, `plans/TENDER.md` and this repo's `CLAUDE.md` — one commit, a paste, not a patch: the red act is canon (D86) and the mark is the payment. 048's text pastes the same way the day its mark is recorded there; without a mark a paste waits, the finding says so, and the baton carries it to ⬡. `canon/CLAUDE.md` (the live wire) is touched by nobody here — D88's line is live already.
- **047's respell** is verified as a respell: the word law and the fixed point re-run at HEAD (`doctrine migrate --summary ~/code/agents` writes nothing), the suite green on the unwrapped tree; a word moved is a rejection.
- **The prune check:** the six rows compress to status + findings pointer under the cap; the batch note dies at the close (D78); `doctrine prune --write ~/code/agents` runs here for the first time, its dry run pasted first.

## Findings

Every command below was run at this desk on 2026-09-09, at `566494f` (048's landing) unless a later commit is named. Re-run, not re-read: every bar's evidence was drawn again from the tree.

**F1 — The bar at HEAD: suite green; the lint one class past nineteen, and the twentieth is belvedere's too.** `bun test` from `doctrine/` **188 pass / 0 fail** at `566494f`, and 188 again after the paste (`bf3c04b`). `doctrine lint ~/code/agents` → `20 failure(s) in 2 class(es) · 171 warning(s)`: 19 `board.cell-cap` (`belvedere/README.md` 10, `belvedere/v3/README.md` 9) and 1 `ledger.merged` at `belvedere/LEDGER.md:344`. The twentieth is 047-F2b — a true positive in a retired building's ledger that the unwrap uncovered, ruled a finding and not a defect at the desk (D88's adoption protocol: *the deltas are findings, not noise*); its repair is a `---`, which is not whitespace, and no charge in this batch may write it. Verdict: the bar's red clause names *a new failure 046's arms raise on a live building*, and this is neither 046's arm nor a live building — the twenty stand with the nineteen until the belvedere purge (the deferred list, D84). `grep 'chars)' | grep -vc belvedere` → 0.

**F2 — 044 verified against its bar: KEPT.** `doctrine boot ~/code/agents | wc -c` → **2,348** (4,606 at its landing — the pack's bulk is the tail entry, and today's tail is 048's Builder's, shorter than the office's); `doctrine boot ~/code/stigmergon | wc -c` → **4,925**. Both under 8,192; the verbatim law is one of the 188. **Ruling (i) did not fire** — no pack exceeds the bar today — and is recorded for the day one does: the defect is the entry's (`ledger.entry-cap`), the pack prints it whole, formula 9 outranks the size bar. `boot .` ≡ the absolute root was proven at the landing and `boot.ts` has not moved since (`git log --oneline eb36cf0..HEAD -- doctrine/src/boot.ts` → 045's baton line only).

**F3 — 045 verified: KEPT — and its census had gone blind to 99 entries.** The arm and the four fields are in the suite; `doctrine boot`'s line reads `Baton — the dispatch · single → ignite G4` at HEAD (shape and type printed, as the bar asked). The census re-run: `lab/045/census.ts` read `LEDGER.md` alone, so after 048's aging it counted agents at **21** entries — 172 city-wide against 045's 265. One side-quest under the Architect's standing grant (`e3c5a5c`): the census reads the pair through `parseLedgerPair`, 048's own reader — one parser, never re-implemented. Re-run: **271** entries (265 + six since), 110 write `Baton —`, 82 read by the line rule, 33 shaped, 117 ⬡; typed mental 8 · visual 1. Untyped, by leading word: `ignite` 25 · `your` 6 · `the` 5 · `summon` 3 · `once` 2 · `a` · `batch` · `both` · `fork` · `is` · `paste` · `resume` · `review` · `rulings` · `tell` (the last new — this batch's *tell the tender to resume*).

**F4 — Ruling (iii): the words proposed for the office's table; none added here.** Citation: this gate's pre-chew (iii) and 045's spec (*the office extends it from the census — the Builder adds no word the record does not show*). Proposed **mental** ← `ignite` (25) · `summon` (3, the older spelling of the same act) · `tell` (1) · `paste` · `resume` · `review` · `rulings` — each a decision to spend or to read, made at a desk with nothing to look at (045-F4's reading, confirmed on the examples). Not proposed as words: `once` (a conditional hiding its verb), `is` (a statement, not an ask), `a` · `both` · `batch` · `fork` (shape markers 045-F1/F2 hide). Two rule asks travel with the words, the office's both: skip a leading article or possessive before reading the noun (045-F3 — eight visual passes at a stroke), and let `:` close a shape marker (045-F2); behind them the anchor (045-F1 — 28 baton lines inside `Next:` the line rule never reaches; D74's holder semantics, out of a Builder's fence). Filed as one inbox entry so the pack surfaces it at the office's next boot.

**F5 — 046 verified: KEPT; its control re-run, not re-read.** Pre-046 source (`git archive cb45a39 doctrine canon`) under HEAD's `test/doctrine.test.ts` + `test/boot.test.ts` and fixtures: **8 fail / 109 pass** — the five 046 law tests (the marked fence, the gate arm ×2, 039-F5's twin skip, the named root), 047's two respell tests (the arrow rule and the partial guard — not yet written at `cb45a39`), and the boot title (the harness's `HEAD no git`, as 046-F named). Nothing else reds; nothing 046 claims passes on the old code. Live, at HEAD: `doctrine lint ~/code/stigmergon` → `0 failure(s)`, `95 kickoffs in 103 work docs`, `080-born-dig.md:244` not named; `doctrine lint …/worktrees/simmy/simmy` → `ledger 2026-09-08 · 1/1 ledgers parsed a tail (44 entries)`, `10 failure(s) in 4 class(es)` — 046-F4's ten, simmy's own (`3 ledger.row · 3 ledger.decided · 3 ledger.next` at `LEDGER.md:1289/1304/1329`, `1 ledger.merged` at `:1200`). **Ruling (ii) did not fire**: `board.gate-kickoff` reddens no live row in either building (046-F1 — both live staffed gates carry their own fence, `G22` is a `⬡-gate`); recorded for the day it does — the rows are named here and stay red, the arm never weakened, no building's doc edited from this gate. **046-F4 routed:** one entry in simmy's inbox (`~/code/universal_robots_sdk/cap-mega/simmy/ISSUES.md`), written and **not committed** — that tree is his, on `dev`, with his own dirty file beside it; the baton names it.

**F6 — 047 verified as a respell: KEPT; the word law re-run over the run's commit, and its one hit is not the unwrap's.** `doctrine migrate --summary ~/code/agents` at HEAD → *agents: already in the current grammar — nothing to migrate* — the fixed point and F3's acceptance test in one line, no `--write` run. Suite green on the unwrapped tree. The word law re-run outside the tool: for each of the 180 files in `7660c30`, the bytes with every whitespace character and `>` removed, before against after — **179 identical, 1 not**. The one is `BOARD.md`, and `git diff --word-diff -w 7660c30~1 7660c30 -- BOARD.md` shows exactly one change: 047's own row, `OPEN — laid 2026-09-08` → `IN FLIGHT — 2026-09-08` — the tender's status mark, dirty in the tree when the Builder committed the run as one act (D81). The rule moved no word; a legal foreign write rode the respell commit, and `.git-blame-ignore-revs` now hides that one status change from blame — trivial, named, not a rejection. `git config --local blame.ignoreRevsFile` → `.git-blame-ignore-revs`; the file carries `7660c30`'s full sha.

**F7 — 048 verified: KEPT; the aging's round trip re-run at the bytes.** `git show a963302~1:LEDGER.md` (218,625 B) against the pair at `a963302`: the archive's body + `---` + the ledger's body is **byte-equal** to the original's body (218,544 B after the header); **99 + 20 = 119** entries by head count. `doctrine boot ~/code/agents` reads `## Ledger — LEDGER.md: 120 entries` at HEAD — 119 plus 048's own. `decisions.size` warns on stigmergon (`41.7 KB in 578 lines`), the only class beside `entry-cap` there. **048's law text carries no ⬡✓ — not pasted**: its four amendments (DOCTRINE §3, §7, §8; the Architect's fourth step) wait in `plans/048-ledger-aging.md` § *The law at the landing*, and the baton carries them. 048-F1 (`--live` compares the file as well as the line) stands as built; 048-F2 (migrate's five ledger structural rules read `LEDGER.md`, not the pair) → the deferred list's parser-residue bullet.

**F8 — The grant: 044's five amendments pasted, one commit (`bf3c04b`).** DOCTRINE §11's Start bullet whole; the mantles README's fourth summons line and the Boot paragraph beneath its fence; the Architect's Orient step; TENDER's read line (its trailing *Then run the batch by this law.* kept — not part of the blessed text, so not a paste's to drop); this repo's `CLAUDE.md` read line. A paste, not a patch: the words are 044's text verbatim; the one form choice is CLAUDE.md's lead clause kept bold as the line it replaced was. After: suite 188, `./sync/check` → `green — 3×2 links, all pointing at canon.` (the wire untouched — `canon/mantles/` and DOCTRINE are not in the sync set), lint unchanged, pack 2,348 B. Beyond the text and not pasted: the Architect charter's own summons fence (`architect.md` § Summons) still reads *then read <the building's docs / board>* — the blessed five name the README's grammar, not the charter's copy; a canon line, the office's, one inbox entry.

**F9 — The prune check (D78), and a lay defect in this doc.** Dry run at HEAD: `1 entr(ies) age out … keeping the last 20` — `grand-architect-21`'s fourth act, 2,127 B, `LEDGER.md 21,864 → 19,737`. This doc's bar said `doctrine prune --write` *runs here for the first time*; 048's own bar had its Builder run it (`a963302`, 99 entries / 197,683 B) — both laid at one sitting, the gate's line predating the member's. No harm and nothing to revert: the check runs here as the standing law, written at the close after this gate's own entry (the count and the guard's line in the ledger). The six rows: 044–048 read under the cap (lint names none of them; the two that measure past 200 bytes are under 200 characters — 041-F2's unit), G4 compresses at this landing; the batch note stays in this doc as history (§3, D78 amended 2026-09-08).

**F10 — Beyond the fence, standing.** `desk/stigmergon-settings.json` and `desk/stigmergon-spaces.json` dirty with no hand on them (G3-F11's trap, still) — untouched, not committed. 044-F5's two board facts — DOCTRINE §4's example table parsing as a board, `018`'s `18c` BLOCKED inside a LANDED charge — print in every pack until the office rules them; one inbox entry. 047-F2b and F2c are belvedere's and die with the purge. Nothing minted: three rulings on this doc's pre-chews, every one cited above.

---

**Kickoff (verbatim):**

```
You are an Architect at fable-high.
Enter by the door — read ~/code/agents/canon/GUILD.md,
wear ~/code/agents/canon/mantles/architect.md,
then read ~/code/agents/MAP.md §7, ~/code/agents/BOARD.md and the ledger tail from 2026-09-08, and execute the charge at ~/code/agents/plans/g4-parser-review.md.
```
