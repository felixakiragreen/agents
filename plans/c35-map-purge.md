# C35 — the master-doc purge

**Status:** OPEN — laid 2026-08-29 · **Depends on:** C34 · **Staffing:** Grand
Architect · fable-max (⬡ at the desk)

## Mission

MAP.md re-cut with C34's blade — every session reads it before any work, so every
token pays rent or moves out. LANDED charges compress to status + date + findings
link; spent batch notes die (the ledger holds them); §8/§9's set keystones compress
to one line each; dead D-references strip; §1–§4, §6, §7, §10 re-read against
current truth. Git holds the history.

## Inputs — read before working

- C34's findings — the blade, the deletion law, the dangling-reference ruling.
- The board's live remainders: do not re-derive — read every Status cell before
  compressing it.

## Kill criteria

- A compression that would delete a live hold, an unmerged branch name, a PENDING
  or DEFERRED annotation, or an OPEN charge's contract: stop — that text survives
  verbatim.
- Meaning must survive whole. This is form's purge, not history's rewrite; the
  charge docs and the ledger already hold what the board sheds.

## Out of scope

The ledger (append-only by law). The Log (voice). `plans/` history. The charters
and the doctrine. Other buildings.

## Findings

*(append here)*

---

```
You are the Grand Architect at fable-max.
Enter by the door — read ~/code/agents/canon/GUILD.md,
wear ~/code/agents/canon/mantles/grand-architect.md,
then read plans/c35-map-purge.md and run the purge with Felix at the desk.
```
