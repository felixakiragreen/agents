# 035 — the master-doc purge

**Status:** LANDED 2026-08-29 · **Depends on:** 034 · **Staffing:** Grand Architect · fable-max (⬡ at the desk)

## Mission

MAP.md re-cut with 034's blade — every session reads it before any work, so every token pays rent or moves out. LANDED charges compress to status + date + findings link; spent batch notes die (the ledger holds them); §8/§9's set keystones compress to one line each; dead D-references strip; §1–§4, §6, §7, §10 re-read against current truth. Git holds the history.

## Inputs — read before working

- 034's findings — the blade, the deletion law, the dangling-reference ruling.
- The board's live remainders: do not re-derive — read every Status cell before compressing it.

## Kill criteria

- A compression that would delete a live hold, an unmerged branch name, a PENDING or DEFERRED annotation, or an OPEN charge's contract: stop — that text survives verbatim.
- Meaning must survive whole. This is form's purge, not history's rewrite; the charge docs and the ledger already hold what the board sheds.

## Out of scope

The ledger (append-only by law). The Log (voice). `plans/` history. The charters and the doctrine. Other buildings.

## Findings

- **F1 — the cut (2026-08-29).** MAP.md 350 → 217 lines, 46,588 → 21,299 bytes at 898ddf2; `doctrine lint ~/code/agents` 0 before and after, 84/84 rows typed. Died: eight spent batch notes (batch 2 · v2 · 08 · 12 · v1.3 · v3 · the grand-architect-11 session note · the deploy batch with its three amendment paragraphs — the ledger holds them), 27 LANDED cells compressed to status + date + findings link, §8/§9's keystone checklists to one line each, §1's origin archaeology, every dead D-number outside protected text. Survived verbatim: the seven OPEN/IN FLIGHT contracts (11 · 15 · 22 · 029–032 · G1), the agents-flow-1 batch note, the peer-messaging DEFERRED, the parked annotations (013-F7 · 014-F5), the branch name `bv/029-summon-harness`.
- **F2 — the desk rulings (⬡, this session).** Row 010's "full panel visual pass PENDING Felix" struck — the pass demonstrably ran: row 011 was laid 2026-08-08 from what it exposed. Row 014's smoke ⬡✓ 2026-08-29 at the desk. Row 008's F3 — the slash-summons colour-carrying agent definitions, DEFERRED 2026-08-06 — killed: superseded by 034-F3's ruling (the rig won the interactive path; a third summons path is redundancy carrying no information). 034's remainder cleared with evidence: the three account `skills` symlinks are gone (`ls` → no such file, ×3 — ⬡'s hand); clearing written on the row.
- **F3 — stale claims the re-read caught (the §1–§4/§6/§7/§10 arm).** Five, each reconciled: §1 claimed the global file syncs "only by hand-sync and discipline" (live ×3 since 2026-08-03); §7 claimed "no bulletin — batches here are sequential" (the 18 wave ran one — `plans/BULLETIN.md`, archival where it lies — and agents-flow-1 is parallel); §2's interactive summoning said "open a session, speak the summons" (the rig fires it, account-routed, name-stamped); the flow-1 note predicted "exactly one lint red (`ledger.baton`)" (lint reads 0 at this session's baseline — retired with the date); flow-1's pre-⬡ order was spent (fork 4 ruled, 028 landed — collapsed to "conditions paid").
- **F4 — a deviation, named.** The manifest presented to ⬡ said Depends cells were untouched; three paid ⬡-gate cells (17 · 19 · 028) in fact shed their spent evidence parentheticals. The gates stay marked paid with dates, the graph is unchanged, and the trimmed text is the exact class 033-F1 proved mis-parses (`,`/`·` inside gate prose). The blessing covered the cut whole; the discrepancy between manifest and diff is recorded here.

---

```
You are the Grand Architect at fable-max.
Enter by the door — read ~/code/agents/canon/GUILD.md,
wear ~/code/agents/canon/mantles/grand-architect.md,
then read plans/035-map-purge.md and run the purge with Felix at the desk.
```
