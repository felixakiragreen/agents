# ⟨ID⟩ — ⟨title⟩

**Status:** OPEN · **Depends on:** ⟨rows | —⟩ · **Staffing:** Builder · ⟨tier⟩ ·
**Blessed:** ⟨who, date — no build before this is filled⟩

## Goal

⟨What exists when this lands, one paragraph.⟩

## Inputs — read before building

⟨Pointers: the blessed spec or design, the laws that bind — master doc §, D-entries.⟩

## Spec

⟨The blessed design, or a pointer to it plus the binding deltas. Implementation choices
inside the spec are the Builder's; anything touching the contract escalates.⟩

## Acceptance criteria — the DoD

- [ ] ⟨Named check + how to run it. Output is pasted here at build time — the DoD is
  measured, not asserted.⟩

## Out of scope

- ⟨The fence. Creep is a bug — adjacent improvements are parked, not made.⟩

## Findings

*(append here — deviations from spec, discoveries, parked adjacents)*

---

**Kickoff (verbatim):**

```
You are a Builder at ⟨tier⟩.
Wear ~/code/agents/canon/mantles/builder.md,
then read ⟨master doc / sections⟩ and ⟨this order's path⟩,
and build it to its DoD.
```
