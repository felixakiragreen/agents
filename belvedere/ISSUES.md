# Issues — Belvedere's incident inbox (D53 pattern)

Field reports, render failures (parser-as-lint, [README §1](README.md)), and
sovereign-inbox entries (D63: `- <date> · Felix (via Belvedere) · <what>`) land
here — Felix's hand, a session's at his word, or the glass's third write
([README §2](README.md)). Entry format (D63): `- <YYYY-MM-DD> · <who> · <what>` —
one bullet per entry; an entry needing evidence becomes a `---`-separated block
opening with that line. This building's Architect sweeps at every sitting: each
entry ruled — folded, cut as a row, rejected, or escalated (canon-shaped entries
go to the canon repo's inbox) — then deleted; entries are committed before they
are drained. A swept inbox is empty.

---

- 2026-08-27 · Builder (B15) · **Lane A and lane B are file-disjoint and NOT commit-disjoint: `git add -A belvedere` from the build chain commits the probe lane's uncommitted work-in-progress.** The batch-5 note calls the lanes parallel-safe on `lab/p5|p6/` vs `glass/`, and the files are disjoint — but both lanes land on `master` in **one shared checkout**, so a scoped-by-directory add is not scoped by lane. Measured by doing it: four of B15's five commits (`7ac9186`, `08b4394`, `36b36d8`) each carried live P6 files (`lab/p6/lib.ts`, `q1.ts`, `wire.ts`, `sink.py`, `arm.ts`, `keys.ts`, fixtures) into a B15 commit message while `p6-live-01` was still running. Nothing was lost, reverted or overwritten — the files are on disk and tracked — but the ledger's attribution is wrong and a Builder can silently commit a Digger's half-written probe. History deliberately not rewritten under a live concurrent session. **Fold candidate: the concurrency plan in a two-lane batch note owes a commit rule, not just a file rule** — scoped `git add <path>` per row, or lane B on a worktree.
- 2026-08-27 · Builder (B14) · **A Dispatcher-tended chain has no legal baton holder, and Belvedere's own ledger tail has linted as a dropped baton for three entries running (B13, P5, B14).** `classifyBaton` knows `session | felix | prose`; a lane whose next row is fired by a *Dispatcher* is none of those, so `Next: **B15 — the Workshop** (…), the Dispatcher's to fire` parses as prose and `batonFails` files `ledger.baton`. Writing `fire B15` instead would make it worse, not better: the rail would compose a live Dispatch button for work the batch note says is the Dispatcher's, which is exactly the class D10 exists to prevent. Fold candidate for the canon baton grammar (B3 F4/F5's neighbourhood — the ask for `Baton.kind` is already filed); the glass changed nothing.
- 2026-08-27 · Digger (P6) · **B15 F7's commit-collision fired again, in the other direction: lane A's `git add -A belvedere` swept lane B's live work-in-progress into two P6 commits.** `4880abe` ("p6: findings") carries B18's in-flight `glass/colors.ts`, `glass/identity.ts`, `glass/hands.ts`, `glass/deck.ts`, `glass/summon.ts` and `lab/b18/colors.ts`; `8e015fd` ("p6: ledger entry") carries `glass/deck-dom.ts` and `glass/deck.client.ts`. Nothing lost, nothing overwritten, history deliberately not rewritten under two live sessions — but the attribution is wrong in both ledgers now, and **the hazard is symmetric**: B15 filed it as a Builder swallowing a Digger's probe, and this is a Digger swallowing a Builder's half-written module. It confirms the fold candidate rather than adding a new one: **a two-lane batch note owes a commit rule** — scoped `git add <explicit paths>` per row (which is what P6 used from this point on), or one lane on a worktree.
- 2026-08-28 · Builder (B11) · **`lab/b17/probe.ts`'s "9 of 9 cells" comparison is brittle against live quota data and fails today, at B17's own landing commit.** An account with no *session* window in flight makes the rig's `summon-usage` print **eight** cells while the deck prints **nine**, one of them `null` — the two agree perfectly (`thg-fgreen sess —` on both sides) and the probe's list-equality fails anyway. Attributed rather than assumed: the identical two failures reproduce byte-for-byte from a detached worktree at `6cd3614` (B17 LANDED), which carries none of B11's code. Not fixed here (adjacent, mantle rule 6): the fix is to compare **per (account, bucket)** and let a bucket neither side has be a match, not to loosen the count.
- 2026-08-28 · Builder (B16) · **B11's finding re-measured, plus one detail it did not carry: when `lab/b17/probe.ts` fails those two assertions it also THROWS before its teardown, leaving its live workspace open.** Re-run whole against B16's client, `bun belvedere/lab/b17/probe.ts` → `FAIL usage is LIVE ×3 accounts … 9 of 9 cells` and `FAIL the probe itself threw`, exactly B11 F8's signature and nothing to do with this row (B13/B14/B15/B20/B10/B11's probes all re-ran **ALL GREEN**). What is new is the residue: `workspace:100 builder-belvedere-78` was still running afterwards, found and closed by hand — a probe's `shut()` has to be in a `finally` that runs before the failure is reported, or a failing DoD run silently costs a live session against the batch's ≤2 concurrency rule (D55). Both halves of the fix belong to whoever takes B11 F8.
