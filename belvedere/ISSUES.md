# Issues — Belvedere's incident inbox (D53 pattern)

Field reports, render failures (parser-as-lint, [README §1](README.md)), and
sovereign-inbox entries (D63: `- <date> · Felix (via Belvedere) · <what>`) land
here — Felix's hand, a session's at his word, or the deck's third write
([README §2](README.md)). Entry format (D63): `- <YYYY-MM-DD> · <who> · <what>` —
one bullet per entry; an entry needing evidence becomes a `---`-separated block
opening with that line. This building's Architect sweeps at every session: each
entry ruled — distilled, laid as a charge, rejected, or escalated (canon-shaped
entries go to the canon repo's inbox) — then deleted; entries are committed before
the inbox is cleared. A cleared inbox is empty.





---

- 2026-08-31 · Builder (B24) · the camera cannot drive a deck that writes the REAL desk, so any
  charge whose bar names both his gestures and his file lands in two runs

`run` refuses `--port` (C17: a probe clicks Dispatch buttons, and on the live deck that fires a real
session) and every twin redirects `DESK_DIR` to the scratch drawer (C17 F1). Both rules are right on
their own; together they mean a pointer gesture can never write `~/code/agents/desk/`. B24's bar 1
was paid in two pieces — `city-arrange-real.probe.ts` for the gestures against the real register,
and `lab/b24/sketch.ts` POSTing to a real Belvedere for the file — and both are honest, but the seam
is a contract question rather than a workaround: either the camera grows a named "real desk" mode
that a probe must opt into out loud, or the bar for a desk-writing surface says two runs.

---

- 2026-08-31 · Builder (B24) · `mintGroup` minted a duplicate id and `parseArrangement` refused
  every live arrangement — two bugs that only a test writing out HIS example and a probe reading the
  receipt could see

Both are fixed and pinned (B24 F3, F4). The transferable half: **a probe that drives a write must
assert the receipt**. The client draws its own optimistic copy the moment a gesture lands, so a page
whose every write is being refused with a 400 looks exactly like a page whose writes all landed. The
live probe passed four assertions before the missing file gave it away.
