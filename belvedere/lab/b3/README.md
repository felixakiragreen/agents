# lab/b3 — the rail's fixture city

Two buildings the live city does not have, so B3's DoD can be measured on shapes rather
than waited for:

- `city/probe-fork/` — a ledger tail whose baton is a **fork**: two exclusive options, both
  instrumented with a fenced summons, one named in the recommendation clause.
- `city/probe-row/` — a board row `R1` whose work doc carries a kickoff fence **and names a
  branch**, so the rail's Dispatch composes `/hands/worktree` → `/hands/fire`.

Point the glass at it: `GLASS_CITY=belvedere/lab/b3/city bun belvedere/glass/server.ts`.
`rail.test.ts` reads the same two buildings — the fixture is the test's corpus and the
smoke's venue at once. `lab/` is not doctrine corpus (DOCTRINE §3), so these two never
appear on the real register.
