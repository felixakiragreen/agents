# the canon map — B20's fixture canon repo

`canonRoot()` is `<city>/agents`, so this directory is the canon the decoder falls back to.
Nothing here is real work; `lab/` is disposable (DOCTRINE §3).

## 1. The register

Every building the walk found.

## 3. The boards

The canon board's row ids are **bare numerals**, which is the whole reason the keyword form
`canon row 17` exists: nothing but the word in front anchors a number.

| ID | Work | Depends on | Staffing | Status |
|---|---|---|---|---|
| 14 | [summon rig — the theater cycle](plans/14-rig.md) | — | Builder · opus-high | OPEN — cut on his ask; theaters are not always directories |
| 17 | the storage experiment — structured source vs schema-markdown | 14 | Digger · fable-high | OPEN — the serialization question is decided by numbers, never decree |
