# A4 — the landed charge

**Status:** **LANDED** 2026-08-30 — MERGED `0000000`; nothing escalated · **Depends on:**
A1 · **Staffing:** Builder · sonnet-medium

## Mission

The finished state — history, so the linter never reads its fences and the deck renders it
dim. It carries none.

## Findings

**F1 — a fixture is only evidence while it is deterministic.** Every byte of this tree is
committed; every timestamp the deck reads is generated at boot (`camera/fixtures/seed.ts`).
