{
	"name": "rate-limit",
	"note": "A rate_limit_event interleaved with the conversation. Noise to the engine's state machine, telemetry to the usage strip — and it must survive the parser either way.",
	"noise": { "rateLimit": true },
	"acts": [
		{ "steps": [
			{ "do": "init" },
			{ "do": "text", "text": "First." },
			{ "do": "tool", "name": "Bash", "input": { "command": "true" }, "result": "" },
			{ "do": "text", "text": "Second." }
		] }
	],
	"golden": {
		"mode": "argv", "paced": true, "seed": 1, "expectExit": 0,
		"flags": ["--model", "haiku", "--permission-mode", "acceptEdits"],
		"turns": ["Say first, run a command, say second."]
	}
}
