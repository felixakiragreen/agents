{
	"name": "schema-needs-input",
	"note": "The gap C6 must close (grammar §5): a session that asks a question is byte-identical to one that finished — same subtype, same stop_reason, zero denials. Only the declared schema makes needs-⬡(question) observable.",
	"acts": [
		{ "steps": [
			{ "do": "init" },
			{ "do": "report", "state": "needs_input",
			  "cause": "Cannot write the config file without: (1) filename, (2) format, (3) contents",
			  "answer": "Provide the filename, format and required keys." }
		] }
	],
	"golden": {
		"mode": "argv", "paced": true, "seed": 1, "expectExit": 0,
		"flags": ["--model", "haiku", "--permission-mode", "acceptEdits", "--json-schema",
			"{\"type\":\"object\",\"properties\":{\"state\":{\"type\":\"string\",\"enum\":[\"done\",\"needs_input\",\"blocked\"]},\"cause\":{\"type\":\"string\"},\"answer\":{\"type\":\"string\"}},\"required\":[\"state\",\"cause\"]}"],
		"turns": ["Write the config file. Report your state."]
	}
}
