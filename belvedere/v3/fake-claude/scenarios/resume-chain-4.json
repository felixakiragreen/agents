{
	"name": "resume-chain-4",
	"note": "Arm A (grammar §3): four invocations, one session id, one transcript. Between turns the session is bytes on disk, not a process — the act index is re-derived from the transcript at every --resume. The adversarial payload rides turns 1-3 byte-exact.",
	"acts": [
		{ "steps": [ { "do": "init" }, { "do": "text", "text": "OK" } ] },
		{ "steps": [ { "do": "init" }, { "do": "text", "text": "OK" } ] },
		{ "steps": [ { "do": "init" }, { "do": "text", "text": "OK" } ] },
		{ "steps": [ { "do": "init" }, { "do": "text", "text": "OK" } ] }
	],
	"golden": {
		"mode": "argv", "paced": true, "seed": 1, "expectExit": 0,
		"flags": ["--model", "haiku", "--permission-mode", "acceptEdits"],
		"turns": [
			"You are a byte-echo probe. For every message I send, reply with exactly the word OK and nothing else.",
			"TURN1 alpha\n\nTURN1 after a blank line\n\tTURN1 tabbed\n\"quoted\" 'single' `backtick` $VAR ${BRACE}\n/not-a-slash-command\n--not-a-flag\némoji ⚡ 中文 ünïcødé\nTURN1 LAST",
			"TURN2 alpha\n\nTURN2 after a blank line\n\tTURN2 tabbed\n\"quoted\" 'single' `backtick` $VAR ${BRACE}\n/not-a-slash-command\n--not-a-flag\némoji ⚡ 中文 ünïcødé\nTURN2 LAST",
			"TURN3 alpha\n\nTURN3 after a blank line\n\tTURN3 tabbed\n\"quoted\" 'single' `backtick` $VAR ${BRACE}\n/not-a-slash-command\n--not-a-flag\némoji ⚡ 中文 ünïcødé\nTURN3 LAST"
		]
	}
}
