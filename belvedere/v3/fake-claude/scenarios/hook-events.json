{
	"name": "hook-events",
	"note": "The hook lifecycle folded into the stream under --include-hook-events: SessionStart, UserPromptSubmit, PreToolUse/PostToolUse around every call, Stop before the result. The census's join key is session_id and it is venue-blind (grammar §5).",
	"acts": [
		{ "steps": [
			{ "do": "init" },
			{ "do": "tool", "name": "Write", "input": { "file_path": "ping.txt", "content": "ping" }, "result": "File created successfully at: ping.txt" },
			{ "do": "tool", "name": "Bash", "input": { "command": "cat ping.txt" }, "result": "ping" },
			{ "do": "text", "text": "ping.txt says ping." }
		] }
	],
	"golden": {
		"mode": "argv", "paced": true, "seed": 1, "expectExit": 0,
		"flags": ["--model", "sonnet", "--permission-mode", "acceptEdits", "--include-hook-events"],
		"turns": ["Write ping.txt and read it back."]
	}
}
