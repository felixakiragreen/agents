# C8 — real-session physics under the engine

**Status:** LANDED 2026-08-30 — **K1 FIRED** on the transcript fallback (F3: the engine's own `--json-schema` makes every turn it fires read `dead` on disk — 48/48 landed turns, ×3 accounts, control green); **K2 did not fire — 63/200 turns, $3.4376/$15**. Stream sensing holds ×3 accounts, all rules; bar 4's matrix tabled with C4 as the control; 9 real-flow runs at 9/9 invariants; three kill classes converge with zero double-ignitions; the summon round trip is lossless both ways. **The bet stands — the redundancy half is thinner than the cornerstone believes.** Two granted engine repairs ride `2fe9d1f` for ratification (F1) · **Depends on:** C11 · **Staffing:** Digger · opus-high — the cornerstone's lay proposed opus-medium; bound up at this lay: kill drills on live accounts and conclusions G4 stands on match the bar C4 proved at opus-high (staffing is the lay's, architect charter)

## Mission

Layer 1 (cornerstone §6): **the same engine, real `claude -p` subjects**, small topologies, ×3 accounts. Measure what the fake cannot — real latency, real cost, real permission physics, kill/survive truth on real bytes — and table the nine capabilities *through the engine* for campaign bar 4, plus the cost inputs bar 5's extrapolation needs. The bet this dig can wound: the engine's sensing on real events.

## Budget — the line (D21: exceeding is a ⬡-fork, never a silent overrun)

- **≤200 subject turns** — ignitions **and** resumes both count (C6 F8) — **and ≤$15** summed live from `result.total_cost_usd`; the first ceiling hit stops the dig: file what stands, escalate. Expected spend ~140 turns / ~$5 (C4 F11: 3.3¢/turn at trivial prompts under live config).
- **Models:** sonnet·low default (P5's unattended floor); haiku·low only for lifecycle-only subjects under `acceptEdits` (C4 F6.3 — legal per (model, posture)). **Postures:** `auto` at sonnet and above (C4's ruling); `bypassPermissions` only inside throwaway scratch venues.
- Every measurement carries its conditions (findings law 7): account · model · posture · load · timestamp.

## Inputs — read before working; do not re-derive

- **C4 is the raw-physics baseline** — [c4-headless-physics.md](c4-headless-physics.md) and [../lab/c4/grammar.md](../lab/c4/grammar.md). This dig measures the same physics **through the engine**; C4's scorecard is the control column.
- C4 F0 — the clean room. The engine already carries it (`spawn.ts`): verify, never rebuild. C4 F6 — silent success: `exit 0` means nothing; the truth is `permission_denials[]`, and posture asked ≠ granted (the engine refuses on mismatch — see it refuse). C4 F5 — needs-⬡(question) is structural only: the `--json-schema` report. C4 F8 — the summon method: private tmux socket (`-L c8`), trust-warm venue inside the fence, `--tools ""` guard; the cmux desktop untouched. C4 F11 — the true cost number. C4 F12 — contamination: subjects wear the account's **live config**, deliberately (comparability with C4's numbers; the real condition); recorded per run; subject prompts never mention this repo.
- C7 F1 — at layer 0 depth rode the harness's rulings; **real subjects land on their own** — a real flow that lands nothing is a signal, not a shape.
- C11's findings — the cursor and the seam; read before igniting anything. Two bind here directly: **C11 F1** — the real-transcript corpus is the live account dirs (mortal: nothing in the repo pins it), *not* `lab/c4/captures/` (those 43 dirs are streams); plan around the account dirs, and expect a cleared one to have taken history with it. **C11 F2** — the transcript fallback reads a tool that merely *failed* as ‹needs-⬡ permission›, measured on real bytes: a torn-stream re-derivation can ask Felix for a permission nobody refused. When a pause's `source` is the transcript fallback, check the stream's `permission_denials[]` where it exists and record any false ask as its own measured event — it is a finding, not noise.
- The fence ([../README.md](../README.md)): subjects killed at landing or the standing set named; resume ids filed in findings; work dirs in scratch, dead at landing.

## Questions

1. **Sensing truth.** The engine's parse rules on real streams — last `result` is the turn, `permission_denials[]`, `init.permissionMode` read-back, the `--json-schema` report — hold ×3 accounts? Evidence per run: run log + stream files + transcript pointers.
2. **Bar 4's matrix.** The nine capabilities (cornerstone §4) exercised *through the engine*, ×3 accounts at sonnet·low, tabled like C4's scorecard with C4 as the control column. The census's ten events accounted for headless: 8 expected present, `Notification`/`PreCompact` structurally absent (C4 F4) — confirm or correct under the engine.
3. **The numbers.** Spawn latency, turn latency, cost/turn distributions — G4 bar-5 inputs, conditions attached.
4. **Kill + survive on real bytes.** (a) Subject SIGKILLed mid-turn: torn stream → the cursor answers for the fired turn (C11 live, on real bytes). (b) Engine SIGKILLed mid-flow: the orphan finishes on disk, the restart adopts or re-derives, converges, **zero double-ignitions** — exactly-once is now a spend property. Each cut class ≥3 runs.
5. **needs-⬡ live.** A permission denial pauses loud with its cause; a question pauses `needs_input` via the report; a posture mismatch (haiku + `auto`) refuses at ignite. One cheap probe each.
6. **The summon round trip.** An engine-born step summoned to a real TUI, one hand turn, resumed headless *by the engine* — one session id throughout, and the cursor survives the hand turn (C11's named hazard, field-tested).
7. **Real flows.** ≥3 topologies (3–6 steps; one carrying a gate or card; one with parallel width ≥2) run to terminal ×3 accounts, `invariants` green per run — a red files with its run dir, log, and transcript pointers (real runs are not seed-reproducible; pointers are the repro).

## Method notes

- Venue: scripts and evidence commit to `v3/lab/c8/` (C4's pattern); telemetry gitignored. ≤10 simultaneous real subjects, ever (C4 Q8's ceiling); concurrency conditions recorded.
- **The harness note (C6 F9, recurs by design here):** reads under `~/.claude*` and some evidence `git add`s may be blocked by the classifier. Do not grind: finish, list the exact commands/paths, Felix runs them by `!` (root ISSUES 2026-08-30 entry; C6/C7 precedent).

## The grant

An engine defect that blocks ≥2 questions may be fixed **in its own commit**, full barrage green as regression, filed as a finding for ratification at review (the door's granted-fix shape; C7 F4's ratified precedent). Anything larger: BLOCKED, escalate.

## Kill criteria

- **K1 — sensing falsified:** a C4-measured parse rule contradicted on ≥3 runs across ≥2 accounts (control: the same drill green elsewhere the same day) → stop that line, file with captures; the engine fix is a new charge, never in-dig beyond the grant above.
- **K2 — the budget:** either ceiling → stop the whole dig, ⬡-fork.
- ≥3 same-class invariant reds across distinct drills → stop that drill class, land what stands.

## Escalation points

- Any auth, login, or trust dialog on an account — only Felix's hands.
- Any settings or hook change wanted in a live config dir — never; file it.
- Nothing third-party is named here, so nothing is authorized (D54).

## Out of scope

Scale beyond 10 simultaneous (C9: 100 fake, ≥25 real) · the console demo (C10) · engine features (C11 landed them; the grant is a repair lane, not a feature lane) · deck code · canon · v2. **Creep is a bug.**

## Findings

*Scripts and evidence: [../lab/c8/](../lab/c8/). Telemetry (run logs, streams, conditions, the off-log tally): `summon/log/v3/c8/`, gitignored. Spend: **63 of the ≤200 subject turns and $3.4376 of the ≤$15** — meter: `bun lab/c8/meter.ts`.*

**K1 FIRED — on one line, and not the one the bet rests on.** The engine's **stream** sensing holds on real bytes ×3 accounts, every rule, every run. The **transcript fallback**'s completion signal is falsified: it reads every turn this engine fires as `dead` (F3). K2 did not fire; the budget held with room. **The bet is not wounded** — but the redundancy half of it is thinner than the cornerstone believes, and the fix is a new charge.

### The scorecard — the nine capabilities *through the engine* (bar 4)

C4's column is the control: raw `claude -p`, 2026-08-29. This column is the same physics driven by `engine/`, sonnet·low under `auto`, ×3 accounts, 2026-08-30.

| # | capability | C4 (raw) | through the engine | evidence |
|---|---|---|---|---|
| 1 | **ignite** | YES | **YES ×3** — the engine's chosen id is honored; 53/53 surviving sessions are named by it on disk | Q1, F8 |
| 2 | **inject** | YES, 24/24 byte-exact | **YES** — a resume is a ruling; driven after a kill (Q4 A) and after a hand turn (Q6) | Q4, Q6 |
| 3 | **sense** | 3 of 4 states; question needs `--json-schema` | **YES, same 3 + the report** — `needs_input` pauses ‹needs-⬡ question› carrying the question | Q5 |
| 4 | **read** | YES, path deterministic | **stream YES · transcript FALSIFIED** — the completion rule calls 48/48 landed turns dead | **F3** |
| 5 | **resume** | YES, lossless, trust-gated | **YES** — summon round trip, one session id, both codewords back | Q6 |
| 6 | **kill** | YES, transcript never torn | **YES ×3 cut classes**, every restart converged, zero double-ignitions | Q4 |
| 7 | **enumerate** | YES ×3 from files alone | **YES ×3** — 53/56 located from the run log alone; the 3 absent were killed before writing | F8 |
| 8 | **scale** | 10/10 at one turn's wall cost | **width 2 costs nothing** (p50 wall 7792 ms vs 8087 ms at width 1); 100 and ≥25 are C9's | Q3 |
| 9 | **survive** | orphan finishes, rests on disk | **YES, 3/3** — the orphan finished onto a stream whose reader was already dead, and the restart landed it | Q4 B |

**The census's ten, under the engine — C4 F4 confirmed, not corrected.** Over 56 c8 subjects, matched into `summon/log/census/census.jsonl` by session id:

```
$ bun -e '…match c8 session ids against the census…'
c8 sids: 56
{ SessionStart: 58, UserPromptSubmit: 58, PreToolUse: 113, PostToolUse: 112,
  Stop: 55, SessionEnd: 55, SubagentStart: 1, SubagentStop: 1 }
```

**8 of ten present.** `Notification` and `PreCompact` absent, both structurally and for C4 F4's own reasons (`-p` exits at turn end and never idles; no session ran long enough to compact). The subagent pair needed a topology that reaches for one — no flat flow does, so it took a deliberate probe (`q2-subagent.ts`).

### F1 — the real arm was unusable; the granted repair, filed for ratification

C11 landed `{real: {}}` and proved nothing in the tree could fire it. Aiming it found two defects, each blocking **every** question in this charge, both taken under the grant, both in one commit (`2fe9d1f`) because they are one repair and Felix's git rule forbids the partial staging that splitting one file would need:

1. **`venueFor(runDir)` hardcoded the config dir** to `<runDir>/config`. But `CLAUDE_CONFIG_DIR` is what selects the account (C4 F0), so every real subject would have spawned into an account with no credentials and no transcripts. `Options.venue` names the account's own dir; `Options.account` feeds the precheck the name its signature has always taken and never been given (`precheck(FAKE_ACCOUNT, …)` was hardcoded).
2. **`fire()` sent `` `${flow.id}/${step.id}` `` as the prompt** — a scenario key the fake ignores and an *address*, not an instruction. A real subject can do nothing with it. `Fired.prompt` now carries the step's own words, defaulted at parse time to the old address, so every committed flow and the whole barrage are byte-unchanged.

Regression, on the settled tree:

```
$ cd engine && bun test                     54 pass · 0 fail
$ ../../glass/node_modules/.bin/tsc --noEmit  exit 0
$ bun barrage/run.ts --runs 1000 --crashes 50
barrage: 1000/1000 green
crash drill: 50/50 converged, zero double-ignitions
mutation check: 9/9 caught
barrage GREEN · 1000 runs · 50 cuts · 9/9 mutants · wall 148.1s   # exit 0
$ cd barrage && bun test                    38 pass · 0 fail
$ ../../glass/node_modules/.bin/tsc --noEmit  exit 0
```

> **Ruled 2026-08-30, the C8 review: RATIFIED — `2fe9d1f` and the follow-up `4d81191`.** Both defects met the grant's own bar (each blocked every question); one commit for one repair in one seam is the git rule applied, not the grant stretched; the parse-time default kept layer 0 byte-unchanged; and the review re-ran the full barrage on the settled tree at its own hand — exit 0. The type-gate follow-up is the landing bar doing its job.

*One follow-up commit, `4d81191`:* the barrage's own type gate caught a hand-built `Fired` literal in `scenarios.test.ts` that the new field made incomplete — a test-only fixture, no runtime path, but the gate is a landing bar and it was red until it was fixed. Both type gates and the full barrage above are the re-proof **after** it. Three hand-built `Fired` literals exist in the tree (two in `engine/test/laws.test.ts`, one here); anyone adding a field to `Fired` must feed all three, and only `tsc` will say so.

**A trap worth the next Builder's while:** the first regression run came back RED at crash seed 1000003, and the red named its own cause — `the restart exited 2 (… was blessed on a different flow — amend and re-bless)`. The barrage spawns `barrage/one.ts` as a **fresh child per run**, so editing `flow.ts` mid-flight had the restart child parse a flow carrying the new field against a log blessed by a child that predated it. Not a defect: invariant 8 working. But it names a real compatibility rule — **adding a field to `Fired` is a run-log break**, because `load()` compares the parsed flow against the blessed one by `JSON.stringify`. Any run log written before this commit cannot be reopened by this engine. Re-run on a settled tree: green, above.

### F2 — the venue precheck, as reserved, would refuse every fence-compliant venue

`venue.ts` reserves the trust precheck for C8 and states the policy: check trust **at ignite**, not at summon, or D20's fallback is missing exactly when it is wanted. Implemented literally (`lab/c8/accounts.ts`, reading the account's own `.claude.json`), it refuses on the first real ignition this campaign ever attempted — correctly, by its own rule:

```
step echo: paused ‹venue› venue trust refused: personal has never accepted the
workspace-trust dialog for …/scratchpad/c8/q1-smoke-personal — a session born
here is unsummonable (C4 F8)
turns 0/2 · wall 1 ms
```

**The v3 fence puts subject work dirs in scratch, and no account has ever trusted a scratch dir.** So an unconditional precheck at ignite and the fence cannot both stand. The measurement that resolves it, and it is C4 F8's own scope: the trust dialog is the **TUI's**, and `-p` skips it. The identical flow in the identical scratch dir, with the precheck off, ignites and lands:

```
step echo: landed report={"state":"done","cause":"","answer":"PONG"}
rule 5/7 session id chosen=c427f775-… sensed=c427f775-… honored=true
rule 4  posture asked=auto granted=auto
rule 1  results=1 dead=false     rule 2  denials=[]     unparsed lines 0
```

So **trust is a property of a flow's intent, not of ignition**: only a step that means to be summonable must pay it. `lab/c8/drill.ts` carries it as a per-flow `summonable` flag, and Q6 — the one drill that actually summons — is the only one that sets it. *Recorded because the engine's own comment says otherwise, and a deck that adopts the reserved policy will refuse every scratch venue it has.*

Two further measurements the deck will want, both free:

- **Trust is inherited by descendants.** A gitignored subdirectory of an accepted repo raises no dialog. Probed by launching the real TUI there and reading the pane: the banner rendered, `Is this a project you created` absent. That gives a venue that is both warm and invisible to git, which is what Q6 used.
- **All three accounts already trust `/Users/felix/code/agents`**, so a warm venue inside the fence exists ×3 without anyone touching a live config dir.

### F3 — **K1: the transcript's completion signal is falsified by the engine's own argv**

`transcript.ts` states the rule and its provenance: *"a turn is complete iff its last conversation row is an `assistant` row carrying no `tool_use` block … Measured on C4's own captures."* None of those captures declared `--json-schema`. **The engine declares it on every step** (`REPORT_SCHEMA` in `argvFor`), and the step report arrives as a `StructuredOutput` **tool call** — whose `tool_result` is then the last conversation row of the turn. The rule's own signature for *"work was in flight when the writing stopped"* is exactly what a cleanly finished engine turn looks like.

The rows of one real turn that landed:

```
13 assistant text
14 assistant tool_use:StructuredOutput     <- the step report
16 user      tool_result                   <- the last conversation row
```

Measured across every real session this dig created — zero extra spend, every byte already bought (`bun lab/c8/q4e-completion.ts`):

```
sessions found 54 · transcripts on disk 51 · missing 3

last conversation row, over every real session this dig created:
   51  user/tool_result

of the 48 turns the ENGINE LANDED from the stream (they demonstrably worked):
  the transcript reader calls 48 of them incomplete -> verdict "dead"
  it calls 0 of them complete
  by account:
    personal       26/26 landed turns read as dead
    thg-fgreen     11/11 landed turns read as dead
    thg-doorbell   11/11 landed turns read as dead

# the control — C4-born real transcripts, no --json-schema declared
  real-q2-a-resume.jsonl   rows=38 last=assistant/text complete=true verdict=worked
  real-q5b-summon.jsonl    rows=44 last=assistant/text complete=true verdict=worked
```

**K1's denominator is met many times over** — 48 contradictions across **three** accounts, with the control green under the same reader on the same day. Per the criterion this line stopped here: filed, not fixed; the engine fix is a new charge.

**What it costs, precisely — and what it does not.** It is a *degradation, never a loss*: the fallback fails **conservative**, pausing ‹dead› where the truth is ‹worked›, so nothing lands wrongly, nothing is double-billed, and the nine invariants stay green through every cut class (Q4). What dies is cornerstone law 5's claim that *"a turn the engine died in front of can be seen to have worked"* — on real bytes it cannot, ever. Two consequences that bind:

- **Q4 class B's success is carried entirely by the durable stream** (C6 F2's ruled fix). Take the stream away and the engine's answer is ‹dead› regardless of what the subject actually did.
- C6's bar *"transcript-only ≡ streamed verdict on all three named scenarios"* was proven against the fake, which writes no `StructuredOutput` row. **The fake is unfaithful in exactly the place this rule lives** — a C5-side finding, and the reason the barrage could never have caught this.

> **Ruled 2026-08-30, the C8 review: the fix is laid as [C13](c13-report-on-disk.md), and it aims higher than repair.** The report's own bytes are *in* the transcript — the `StructuredOutput` tool call's input — so the completion rule is not merely trued: the fallback gains the report itself, and a turn the engine died in front of can **land from disk**, restoring cornerstone law 5's redundancy claim stronger than C6 proved it. The fake becomes faithful in the same charge (the barrage must guard this class forever), and C11's cursor regression signal (F7's caveat) is restored with it. C13 precedes C9 — G4 does not convene on a falsified capability row.
>
> *Corrected 2026-08-30, the C13 review (C13 F1): the "barrage guards this class forever" clause was this ruling's claim and it is measured false — the durable stream means a torn stream and a complete transcript essentially never coexist (1 fallback read in ~2,300 turns, 0 landings from it), so the class is guarded by C13's deterministic pair. The fake's faithfulness was delivered; the frequency claim was the desk's, and the correction is the desk's too. Second occurrence of the shape (C11 F4); the sixth cut family — a cut that kills the subject between its closing pair and its `result` row — is deferred, tracked on the board's deferred list, promoted if the shape recurs a third time.*

### F4 — `auto` is not a restrictive posture headless, and the denial pause cannot be induced at it

The charge's default posture is `auto` at sonnet (C4's ruling). A three-rung ladder, each rung a real turn, each verified to have actually *done* the thing:

```
   outside  exit=null is_error=false denials=0 [] -> landed
   bash     exit=null is_error=false denials=0 [] -> landed
   fetch    exit=null is_error=false denials=0 [] -> landed

$ cat /tmp/c8-denial-probe.txt
HELLO-C8
Write     {"file_path":"/tmp/c8-denial-probe.txt","content":"HELLO-C8"}   # OUTSIDE the workspace
Bash      {"command":"/usr/sbin/networksetup -listallnetworkservices"}
WebFetch  {"url":"https://example.com"}                                   # the public internet
```

**Under `auto`, a step gets filesystem-wide writes, arbitrary shell, and network egress**, and the engine's most dangerous parse rule — rule 2, the one C4 F6 called the single most dangerous finding of that dig — **cannot be exercised at the charge's own default posture**. `acceptEdits` is the pair that refuses, and under it the silent success appears exactly as C4 described:

```
## acceptEdits-bash (asked acceptEdits, init granted acceptEdits)
   the substrate's own success signals: is_error=false results=1
   permission_denials[] = [{"tool":"Bash","toolUseId":"toolu_01Dcsee…"}]
   the engine's verdict:  paused ‹needs-⬡ permission, needs-⬡ question› —
     permission denied: Bash; The bash command `/bin/date +%Y` requires user approval…
```

`is_error: false`, one clean `result`, and the step **did not land**. Rule 2 holds on real bytes. Note the pause names **both** causes — C6 F1's law working: the subject also reported `needs_input`, and reporting one would have hidden the other.

*This is a deck-facing safety input, not just a measurement:* "auto" reads like a cautious default and is not one.

### F5 — the posture gate protects a live fact, re-measured today

Bless refuses `(haiku, auto)` before a turn is spent; the substrate's own answer, taken raw because the engine correctly will not spend it (one off-log haiku turn):

```
## posture — bless refused: YES
   step h: (haiku, auto) is refused: haiku is granted `default` for `auto` and does no work
   turns spent: 0
## posture control — the substrate's own answer, one raw haiku turn
   asked auto · init granted "default" · match false
```

C4 F6.2 stands at 2026-08-30. Note the read-back gate (`granted !== asked`) is exercised by **every** turn in this dig and never fired — under v3's three legal postures at sonnet, asked always equalled granted (54/54 turns with a result).

### F6 — kill and survive on real bytes: three cut classes, and the one that matters

`bun lab/c8/q4-kill.ts <A|B|C> personal 3`, ≥3 runs each, one 2-step flow (a 12-second Bash turn to cut into, then a step that must still follow it):

| class | the cut | what the engine had | outcome, 3/3 |
|---|---|---|---|
| **A** | the **subject** SIGKILLed mid-turn, engine alive | a torn stream it watched die | `results=0 dead=true` → paused ‹dead› → ruled resume on the same session → **all landed, 9/9 green** |
| **B** | the **engine** SIGKILLed mid-turn (exit 137), subject left running | a **complete** stream written by an orphan after its reader died | `results=1 dead=false` → restart adopted from the stream → **all landed, 9/9 green, 1 ignition per step** |
| **C** | **both** killed | a torn stream and no reader | fell to the transcript → paused ‹dead›, downstream pending → **9/9 green** |

**Class B is the campaign's headline.** C6 F2's ruled durable stream was designed for exactly this and could only ever be simulated at layer 0. On real bytes: the engine dies mid-turn, a real `claude` keeps writing to a file whose opener is a corpse, and the restart reads a finished turn and lands it. **Exactly-once held in every run** — `ignitions {"cut":1,"after":1}` — which at layer 1 is a *spend* property, not just a correctness one.

Class C's terminal state (paused ‹dead›, `after` pending) is **correct, not a failure to converge**: the turn genuinely never finished, and the engine refuses to guess. My drill's "0/3 converged to all-landed" line is the drill's expectation being wrong for that class, not a red — invariants were 9/9.

### F7 — the cursor across a hand turn, field-tested (Q6)

The summon round trip, engine-born, in its natural shape rather than a contrivance: a step that cannot finish without a human pauses, is summoned, answered by hand, and resumed **by the engine**.

```
1. headless ignite -> paused ‹needs-⬡ question›   asked: What is the release name…
   transcript rows 18
2. summoned to a real TUI (tmux -L c8)
   HISTORY RENDERED: the headless codeword is in the pane = true
   trust dialog present = false
3. HAND TURN: transcript rows 18 -> 35 (+17 rows the engine never fired)
4. resumed HEADLESS by the engine -> landed
   answer "HEADLESS-C8-ALPHA, TERMINAL-C8-BRAVO"
   ROUND TRIP: headless-born codeword=true · TUI-born codeword=true
   ONE SESSION THROUGHOUT: … same=true
5. the cursor across a hand turn
   cursor at ignite 0 · cursor at resume 35 · rows after the hand turn 35
   the recorded cursor counted the pane's rows too: true
   past the recorded cursor -> rows=16 turns=1 (the engine's own resumed turn)
   a reader resuming from "where MY last turn ended" (row 18) -> rows=33,
     which swallows the hand turn
   turns 2/4 (engine) + 1 by hand · invariants 9/9 green
```

**C11's named hazard, on real bytes.** The recorded cursor lands on 35 — the row count *including* seventeen rows no engine turn produced — while a reader addressing its own last turn reads from 18 and swallows the pane's work whole. D20's fallback is lossless in both directions, and the cursor is what makes the second direction re-derivable.

*A caveat this dig owes C11's kill criterion:* class C and class D both exercised the cursor with an **empty** slice, so the cursor was proven to *slice correctly* here, never to disagree with a row-0 read on a torn resume — F3 is why. With every engine turn reading `dead` from row 0 anyway, the two answers now agree by accident. **F3 has therefore taken the cursor's real-bytes regression signal with it**, which is a second reason the F3 fix wants its own charge.

### F8 — enumerate, and the id the engine chose

The run log alone locates the subject in the account dir — the id the engine picked names the transcript file (C4 F2's rule, through the engine):

```
  personal       run log names 34 sessions · 31 found in the account dir from the log alone
  thg-fgreen     run log names 11 sessions · 11 found in the account dir from the log alone
  thg-doorbell   run log names 11 sessions · 11 found in the account dir from the log alone
```

53/56. The three absent are class-C subjects SIGKILLed before they wrote a conversation row — correct, and the same three `q4e` reports as missing.

### F9 — the numbers (bar 5's inputs), and the ceiling that actually binds

`bun lab/c8/q3-numbers.ts`, over all 61 recorded turns (54 with a result row; the rest were cut by a drill):

```
## latency
  api (model only)        n=54  min 2035  p50 5253  p90 7273   max 15571 ms
  turn (subject's own)    n=54  min 2142  p50 5965  p90 11017  max 17415 ms
  wall (engine to engine) n=54  min 3650  p50 8087  p90 13571  max 20696 ms
  engine + process start  n=54  min  956  p50 2489  p90 3489   max  3612 ms

## cost
  cost/turn   n=54  min 0.0154  p50 0.0645  p90 0.0770  max 0.1354 USD
  total       $3.4376 over 54 turns · mean $0.0637/turn
  C4 F11's control  $0.0331/turn (51 turns, haiku+sonnet, 2026-08-29)
```

Three things G4 needs from this:

1. **`p50 2489 ms` of engine + process start.** C4 F3 measured the price of a process per turn at ~2.3 s against a live process. Through the engine, on a loaded machine, it is the same number. **Cornerstone §3.2's tax is confirmed and is not growing.**
2. **A sonnet·low turn costs ~6.4¢, about 2× C4 F11's blended 3.3¢** (C4's mean mixed haiku in). On the strictly comparable set — the q7 topologies, one prompt shape, sonnet·low under `auto` — the three accounts agree within 15%: personal $0.0648, thg-fgreen $0.0577, thg-doorbell $0.0566 per turn.
3. **The dollar ceiling binds long before the turn ceiling.** At the measured mean, 200 turns is **$12.74** — inside $15 by a margin thinner than the spread between accounts, and a charge that drew heavier prompts than these trivial ones would hit $15 first. *A future real-session budget line should lead with dollars and treat turns as the secondary bound.* Extrapolation:

```
   10-step flow  $0.64      50-step flow  $3.18
   25-step flow  $1.59     100-step flow  $6.37
```

Width 2 costs no extra wall time (p50 7792 ms vs 8087 ms at width 1), consistent with C4 F10 at N=10.

### F10 — grammar drift: three event shapes C4 never saw, and C4 F1 not reproduced

The subagent probe (`q2-subagent.ts`, one turn) went looking for C4 F1's load-bearing surprise — *"one invocation can emit more than one `result`"* — and did not find it:

```
  parse rule 1: results in this ONE process = 1
  the shape of it: system/init -> result/success
  stream event types:
      3 assistant · 3 user · 1 system/hook_started · 1 system/hook_response
      1 system/init · 1 rate_limit_event · 1 system/task_started
      1 system/task_updated · 1 system/task_notification · 1 result/success
```

The subagent ran (the census logged `SubagentStart`/`SubagentStop`) and completed **inside** the turn — one `init`, one `result`. Parse rule 1 (*read to EOF, take the last*) is unharmed and remains the safe rule either way; but **the surprise that motivates it did not reproduce under today's binary**, and a future charge tempted to simplify rule 1 should re-measure rather than trust C4 F1's shape. One attempt only — recording what was seen, not claiming absence.

Three shapes not in [grammar.md](../lab/c4/grammar.md), none of which the sensor reads (it consumes `system/init` and `result` and ignores the rest, so this is grammar bookkeeping, not a defect): **`rate_limit_event`**, the **`system/task_started|task_updated|task_notification`** trio, and **`system/hook_started`/`hook_response` arriving without `--include-hook-events`** — which is C5 F2's leak, confirmed on real bytes through the engine.

### The questions, answered

1. **Sensing truth ×3.** Stream rules **hold**, all of them: the chosen session id is honored (53/53 on disk), `init.permissionMode` reads back equal to the ask (54/54), the last `result` is the turn, `permission_denials[]` is the truth signal (F4), the `--json-schema` report parses. The transcript fallback's completion rule does **not** (F3, K1).
2. **Bar 4's matrix.** Tabled above. 8 of the census's ten present; `Notification` and `PreCompact` structurally absent — C4 F4 confirmed under the engine.
3. **The numbers.** F9.
4. **Kill + survive.** F6 — three cut classes, ≥3 runs each, every restart converged, **zero double-ignitions anywhere**.
5. **needs-⬡ live.** All three: posture refused at bless (F5), question paused ‹needs-⬡ question› carrying its cause, denial paused ‹needs-⬡ permission› — but only at `acceptEdits`, because `auto` denies nothing (F4).
6. **The summon round trip.** F7 — lossless both ways, one session id, cursor survives the hand turn.
7. **Real flows.** Three topologies × three accounts, **9 runs, 9/9 invariants green each, zero reds**, and the work verified on disk rather than taken from the engine's word:

```
  q7-chain-personal      3 turns  alpha/beta/gamma.txt OK    9/9 green
  q7-gate-personal       3 turns  gate + card ruled          9/9 green
  q7-fan-personal        4 turns  width 2, join OK           9/9 green
  … identically on thg-fgreen and thg-doorbell (9 runs total, 0 reds)
```

### C11 F2, checked as the charge asked

Every transcript-sourced pause in class C and class D was checked against its stream's own `permission_denials[]`. **No false ask was observed** — `asked for permission=false · the stream's own denials=0` in 3/3 class-C runs. C11 F2's hazard is real but needs a turn that reached a failing tool before dying; these cuts landed earlier than that. Not disproven — **unexercised**, and F3 now outranks it: a fallback that calls every turn `dead` never reaches the denial branch at all.

### Subjects and hygiene

All subjects are dead: every drill ran to a terminal state or was SIGKILLed by its own drill; `tmux -L c8 kill-server` closed the only pane, and the cmux desktop was never touched. No standing set. Session ids are in the run logs under `summon/log/v3/c8/*/run.jsonl` (gitignored telemetry, mortal). Work dirs are in scratch and die with it, except the Q6 venue, which is a **gitignored** dir inside the repo (`summon/log/v3/c8/venue/`) — chosen because the summon needs trust; `git status` is clean of it. Concurrency never exceeded **2** simultaneous real subjects, far under C4 Q8's ceiling of 10. One artifact was written outside the fence deliberately — `/tmp/c8-denial-probe.txt`, the F4 ladder's proof that an out-of-workspace write was granted — read as evidence, then removed at landing.

Verified at landing: `ps` shows **no c8 subject alive** (every live `claude` on the machine carries cmux's `--settings` hook block and a `-n`/`--resume` name — Felix's own interactive sessions; a c8 subject carries `--session-id <uuid> --json-schema …` and none remain), `tmux -L c8 ls` → *no server running*, no orphaned `sleep` from the kill drills.

---

```
You are a Digger at opus-high.
Enter by the door — read ~/code/agents/canon/GUILD.md,
wear ~/code/agents/canon/mantles/digger.md,
then read ~/code/agents/belvedere/v3/README.md
and execute the charge ~/code/agents/belvedere/v3/plans/c8-real-session-physics.md.
```
