# G4 — the verdict

**Status:** OPEN — convenes on campaign bars 1–6, all LANDED 2026-08-30 · **Depends on:** C9, C10 · **Staffing:** ⬡-gate — Felix's hand and Felix's word; nobody ignites past it. Pack assembled by the batch-8 tender, 2026-08-30.

## The four rulings — his alone

1. **The substrate verdict** — is headless (`claude -p`, turn-per-invocation, state on disk) the engine venue?
2. **v2's fate** — what of the live deck and the v2 engine survives; what is superseded-in-place (D19's framing).
3. **The migration shape** — how v2's flows, boards, and organs move onto the v3 engine, and in what order.
4. **The viewport call** — the Chat as primary, summon-to-terminal as fallback (D20's bet, now measured); cmux's remaining role.

## His hand — bar 6's visual pass

```
bun belvedere/v3/console/rehearsal.ts personal      # the scripted pass, ~6 turns, ~25¢
```

then by hand: `bun belvedere/v3/console/cli.ts list <runDir>` · `read` (note the quota line — the in-band gauge) · `send` an answer into the paused step · `summon --tmux` · type a turn · `return`. Every verb refuses in kind on a wrong target; nothing needs this session.

## The pack — six bars, all measured

| bar | claim | evidence |
|---|---|---|
| 1 | 1,000 seeded runs, nine invariants machine-checked, green | [C7](c7-fuzzer-barrage.md) — re-proved at every later review; latest: 1000/1000, 26,279 steps |
| 2 | ≥50 crash cuts, every restart converges, zero double-ignitions | [C7](c7-fuzzer-barrage.md) bar 6 — 50/50, all five families; real-bytes analog [C8](c8-real-session-physics.md) F6 (3 cut classes ×3) |
| 3 | the oracle seen to fail, 9/9 mutant classes | [C7](c7-fuzzer-barrage.md) bar 7 — every control green |
| 4 | nine capabilities ×3 accounts through the engine; ten census events accounted | [C8](c8-real-session-physics.md) scorecard — 9/9 (the transcript-read row falsified there, **trued at [C13](c13-report-on-disk.md)**: 50/50 landed turns re-derive to landings from disk) |
| 5 | 100 simultaneous fake green; real ≥25 measured; the cost table | [C9](c9-scale.md) — 12/12 at width 100 (in-flight measured, not asserted); 25-burst green ×2, no ceiling; Q3's table |
| 6 | list · read · send · summon · return, his hand | [C10](c10-console-demo.md) — five verbs, rehearsal green, refusals in kind |

**Campaign spend, total: ~223 real subject turns, ~$9.64** (C4 $1.69 · C8 $3.44 · C12 $0.13 · C9 $3.97 · C10 $0.41) — every charge under its line, zero ⬡-forks fired.

## What the verdict should weigh — the honest limits

- **`auto` is not a cautious posture** (C8 F4): a headless step under it was granted an out-of-workspace write, arbitrary shell, and network egress; the denial pause exists only at `acceptEdits`. Any "unattended by default" story must pick its posture with eyes open.
- **The false ask** (C11 F2, unfixed by design): a *failed* tool re-derived through the transcript fallback reads ‹needs-⬡ permission›. Narrow (torn stream + failing tool), conservative, but a board could one day ask you for a permission nobody refused.
- **The cost model inverts intuition** (C9 F2): a cold one-shot turn is ~2× a warm one; width is *cheaper per turn* than serial ($0.1355 → $0.0453 at 25). Real sonnet·low ≈ 4.5–6.5¢/turn depending on shape.
- **The redundancy triad is proven but young**: run log, stream file, and transcript are each independently sufficient for a landed turn (C13's end-to-end law-5 drill) — yet the barrage structurally cannot fuzz the transcript-landing path (C13 F1); deterministic tests guard it.
- **Five deferred items** ride the [README](../README.md) deferred list — none blocks a verdict; the account-on-`ignited` seam (C10 F5) is the first the deck build hits.
- **The in-band quota gauge** (C9 F5): every real turn carries `unifiedWindows.*.utilization` — the deck's usage strip can ride the engine's own telemetry.

## The tender's read — recommendation where evidence carries, taste marked

The evidence supports **substrate: YES** — every capability measured, every invariant fuzzed, crash-redo proven on real orphans, scale to 25 real / 100 fake with headroom, at ~5¢ a turn. Rulings 2–4 are operational and taste — what your daily hands want — and carry no recommendation from this desk.

## The verdict, recorded — ⬡✓ 2026-08-30, Felix, in-session

His hand ran the rehearsal first: exit 0, 9/9 invariants, 3/3 landed, the round trip true both ways, "REHEARSAL-ALPHA, REHEARSAL-BRAVO", ~$0.20.

1. **Substrate: YES — blessed** ("bless"). Headless (`claude -p`, turn-per-invocation, state on disk) is the engine venue. The rider is law: **naked `-p` without the engine is not an approved way to run unattended work** — the substrate lies politely (C4 F6, C8 F4); the engine's parse rules, posture refusal, and loud pauses are what make it safe.
2. **v2's fate: superseded-in-place** ("rec" — the recommendation taken). The v2 engine retires; the deck's organs survive (census, City View, baton rail, shelf, usage strip, inbox — the diagnosis never condemned them, cornerstone §2); the deck's hands re-point to the v3 engine; cmux retreats to viewport candidate.
3. **Migration: strangler, engine-first — simplified by his own fact.** His word, verbatim: *"there are no existing v2 flows. (Well, there is 1, but it was stopped mid flow because of problems) I am willing to abandon it."* agents-flow-1 is **abandoned**; with nothing alive to strangle, the v2 engine retires immediately. The shape: the seams charge first (the account field on `ignited` · the trust read into `venue.ts` · the answer-then-land scenario), then the deck's hands grow the v3 lane, then the Chat chapter on the engine's read/inject (D20's my_checklist pattern) — every step a charge, the barrage as standing regression. The abandoned flow's remains (the stopped run log, the `bv/c29-summon-harness` worktree, the flow-1 inbox entries) are the migration lay's first sweep — D19's deferrals unfreeze with this verdict.
4. **Viewport: the Chat is primary** ("Yes, Chat as primary"); summon-to-terminal the fallback, measured lossless both directions; the console the interim hand until the Chat chapter lands.

**The campaign is complete. The keystone is set, 2026-08-30.**
