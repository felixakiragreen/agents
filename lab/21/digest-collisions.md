# collision-flagged (294) + polysemy top

## flagged

- **row** [collision:row]
  “a horizontal grouping of two paired unit-selection controls in a sett…” | “one item/node in the outliner document tree, rendered as one visible …” | “a selectable line item in a Swing UI table/tree, unrelated to a dispa…” | “a registry entry for one simmy instance, carrying attribution metadat…”
- **gate** [collision:felix-gate collision:Felix-gate collision:gate collision:gate(review-row) collision:gate-checkpoint]
  “to be the judgment a following row must wait behind” | “a judgment step standing between two rows” | “an automated pass/fail typecheck barrier (bunx tsc --noEmit), distinc…” | “an automated typecheck pass/fail barrier” | “the automated offline typecheck pass/fail barrier”
- **gated** [collision:gate collision:BLOCKED]
  “to make a charter expansion contingent on a prerequisite condition be…” | “a helm-item category tag marking a dependency blocking downstream wor…” | “a helm-frame status meaning the underlying product issue is open and …”
- **land** [collision:landed collision:LANDED collision:LANDED(row-status) collision:LandingView]
  “the board's own status text marking a step done, as read by the engin…” | “to arrive and come to rest in a file — of a report, not a row” | “to arrive and be filed in the inbox — not a row reaching LANDED” | “a piece of vocabulary becoming finalized/ratified as part of a D-entr…” | “a document becoming final/adopted (here, before it was published)”
- **finding** [collision:findings]
  “one individual conclusion a session reached, not the evidence record …”
- **green** [collision:red]
  “the passing state of a named check suite” | “a check that passed, evidence-grade” | “a harness state: all assertions passing (green) or some failing (red)”
- **session** [collision:sitting collision:bee collision:session]
  “one Claude conversation from boot to clear — the unit definitions loa…” | “a single running Claude Code agent process/instance, identified by se…” | “used loosely as a synonym for 'sitting' — the same continuous stretch…” | “one of the desk's three named send-targets/routes (alongside →inbox a…” | “a running instance of the engine/UI under test, distinct from a Claud…”
- **ruling** [collision:verdict collision:D-entry collision:ratify collision:bless]
  “a decision Felix hands down that binds without further argument” | “a decision made on a live fork, weaker ceremony than a D-entry” | “the recorded decision an office makes when applying Felix's gesture t…” | “Felix's personal decision act on phase acceptance, recorded as its ow…” | “Felix's phase-acceptance verdict, recorded as its own ledger artifact”
- **pass** [collision:PASS(assertion marker) collision:PASSED collision:pass(verdict-token) collision:pass(test-outcome)]
  “a named check returning success” | “one sweep of human attention over an output” | “count of passing unit tests in a suite run” | “Felix's approval/proceed gesture on a waiting flow-node card” | “a review/inspection sweep of finished work (e.g. Felix looking over t…”
- **PASS** [collision:PASSED]
  “the harness's assertion-success marker, prefixing a verified evidence…” | “the token marking a probe arm's measured outcome as matching its expe…” | “a test/perf-budget passing verdict token, distinct from the canon boa…” | “per-check pass marker prefixing individual verification-rig assertion…” | “per-check pass marker, a token distinct from canon PASSED”
- **arm** [collision:arm collision:arm (a test arm) collision:arm (the mechanical arm of a law) collision:control arm collision:arm(control-branch)]
  “one measured run or condition of an experiment” | “one branch of a probe — the control arm versus the test arm” | “enable a gated, potentially dangerous action once its preconditions a…” | “to authorize a declared flow to begin executing under the engine” | “to authorize a flow (or one of its steps) so the engine may fire it a…”
- **verdict** [collision:verdict collision:verdict-annotations collision:PASSED]
  “a conclusion the rest of the campaign will inherit” | “an authoritative statement about how the built system behaves” | “PASSED / MERGED / BLESSED — the outcome token of a judgment row” | “a conclusion drawn from measurements” | “same object as the Felix-card: the specific paused engine node presen…”
- **board** [collision:board collision:board(work-table) collision:board(work-state-table)]
  “the sections-holder object for a meeting (frozen snapshot or the live…” | “a physical Raspberry Pi device; a homonym collision with the canon-gl…” | “the physical Raspberry Pi device under test” | “here 'board' refers colloquially to this bulletin's running thread of…”
- **work order** [collision:order]
  “the Builder's work doc: a blessed spec plus a measurable DoD” | “a scoped construction doc spun out when a session cannot finish” | “hexwright's spelled-out name for a canon 'order' (Builder work doc); …” | “a small, review-cut follow-up task appended to an already-landed row'…”
- **law** [collision:D-entry collision:law]
  “hexwright's convention of naming binding rules '<X> law' — its own nu…” | “a binding, permanent project-level rule graduated from a campaign's o…” | “a hard, numerically-specified constraint recorded in the repo's CLAUD…” | “a named durable rule cited by an Arborist decision number (A-prefix),…” | “suffix naming a standing rule from a spec section, cited by name acro…”
- **RULED** [collision:BLESSED]
  “marked as an already-adjudicated, accepted failing result rather than…” | “the fixed attribution+date+verdict markup used to record Felix's (or …” | “the same RULED(date): verdict formula seen in the naming/node-param d…” | “a decisive settlement of an open fork/question, distinct from ratify/…” | “Felix's decision on a forked question has been made and is now binding”
- **rule / ruled** [collision:bless collision:countersign]
  “Felix (or an Architect) issuing an authoritative, binding decision on…”
- **work** [collision:arm]
  “the board column holding the row's one-line content” | “the totality of a building's past-and-planned effort, unified into on…” | “any session's doing in the repo, as the thing the doctrine governs” | “a scope class sizing a task against a session or a campaign” | “work classified by which office owns the authority to do it”
- **fire** [collision:fire collision:fire(dispatch-sense) collision:fire(dispatch)]
  “to launch a row's session from its work doc's own kickoff fence” | “a kill criterion being triggered by the evidence” | “an agent being launched into flight” | “dispatch a board row's work doc as a live session” | “the named endpoint of the hands' wire contract that launches a new di…”
- **agent** [collision:bee]
  “the smallest ontology tier — an individual running session”
- **landed** [collision:LANDED collision:LANDED(board)]
  “extends the seeded row-status verb metaphorically to any successfully…” | “the engine's own two-sensor definition of a step's completion, distin…” | “interim dual-sensor definition of a flow step's landed state, distinc…”
- **tier** [collision:tier]
  “a permanent automated test-suite layer that asserts performance budge…” | “a permanent budget-asserting product test-suite layer — same product …” | “a test-case execution classification level (e.g. robot-tier, machine)…” | “a test-case execution-cost/capability classification level, distinct …” | “scheduling driven by test-case execution classification (T0/T1/T2), u…”
- **window** [collision:window collision:window (session) collision:window(GUI)]
  “one Mentat sitting, framed as an opening rather than a session” | “a bounded period of uncontended host conditions” | “the period during which agents are worktree-isolated from each other” | “a bounded byte range read from the start of a session transcript file…” | “a bounded period during which multiple sessions/rows run concurrently”
- **founding window** [collision:bee]
  “the session that founded a project, addressable later as an office's …” | “the first session summoned to originate a new artifact (here, a proje…” | “a personified individual Architect session, treated as having its own…”
- **DONE** [collision:LANDED]
  “board status tokens explicitly forbidden in favour of the five states” | “a work-order document's own completion status token, distinct from th…”
- **cut** [collision:strike collision:cut collision:cut(board-sense) collision:cut-board-verb]
  “to author a row into existence on the board” | “the act and product of composing a batch, as a noun” | “authoring a row, batch or entry into existence” | “create/spawn a new board row” | “to author/commit board rows, a batch, or a work doc into existence”
- **bless** [collision:bless (approve a spec) collision:bless]
  “approving a spec so a Builder may build against it” | “disputed at this spot: whether 'bless' (approve a spec) should collap…” | “Felix's own word used to countersign a decision, not to approve a spec” | “to approve and commit an updated golden test fixture as the new expec…” | “recording a test's current output as the golden baseline via the --bl…”
- **rules** [collision:verdict]
  “Felix personally deciding whether a phase passes acceptance — distinc…”
- **cell** [collision:cell collision:cell(orbstack-machine)]
  “one parallel agent slot within a dispatched batch” | “one named simulator VM/instance allocation dedicated to a single row” | “an isolated provisioned simulator instance-slot within the shared sim…” | “a named instance of the simmy test harness environment” | “an auto-provisioned pool/execution environment that simmy shards test…”
- **definition of done** [collision:Acceptance]
  “the enumerated, checkable close conditions of a campaign or row” | “the campaign-wide list of criteria that must all be met for the whole…” | “the brief section enumerating the measurable acceptance criteria a Bu…”
- **close** [collision:close collision:close-board]
  “a row reaching a terminal state — after which its findings freeze” | “the act of ending a session properly” | “tear down/release a cmux workspace opened by a fired probe session” | “the formal end-of-batch verification ritual (checks plus a live proof…” | “literal act of closing a cmux workspace as venue hygiene after a prob…”
- **canon** [collision:canon]
  “the cross-project body of law kept in ~/code/agents” | “the body of ratified, signed documents in the agents repo” | “hexwright's own in-repo aesthetic-rules folder, explicitly disambigua…” | “hexwright's ART canon (aesthetic ruleset section), a second sense of …”
- **fence** [collision:the fence collision:fence collision:fenced (in a code fence) collision:fence (scope boundary) collision:fence(barrier-sense) collision:fence(kickoff-marker-sense)]
  “a code-fenced block holding text meant to be pasted, not read” | “the scope boundary inside which a Builder decides freely” | “a mantle's write-authority boundary” | “a hard boundary/constraint established by a ratified decision” | “the formal boundary defining which write actions a building's agents …”
- **STOP** [collision:KILLED collision:STOP-kill-condition]
  “allcaps imperative marking a hard halt point in a procedure” | “a hard-halt outcome triggered when a kill criterion fires, distinct f…” | “a hard-halt kill condition: if a named dangerous outcome (e.g. data l…” | “a scheduled deadline/cutoff time for a batch, after which undelivered…” | “an all-caps imperative marking an immediate halt-and-escalate directi…”
- **venue** [collision:venue collision:venue (a disposable VM, D55) collision:venue (the doc a board lives in, D45) collision:home]
  “the live machine or environment work runs on” | “a live machine or container minted for a row's work” | “the environment/location in which a test is exercised” | “the working environment (cmux workspace + scratch dir) a probe used a…” | “the recorded environmental footprint (branch, worktree, account, work…”
- **log** [collision:ledger]
  “the Grand Architect's per-seat diary file, a memory outliving one win…” | “a per-campaign dated running record of sessions, decisions, and next …”
- **relay** [collision:relay collision:relay(verb)]
  “fixed heading format announcing one bulletin entry's routing from a l…” | “the handoff of verbatim summons text from one window to the next” | “a prior relayed message/entry being corrected, noun use rather than t…” | “a named ledger section recording whether any escalation from one seri…” | “noun: one instance of a bulletin-carried notice between parallel Buil…”
- **ruled by** [collision:verdict]
  “Felix's personal decision act, used as a D-entry decider verb instead…”
- **dirty / clean** [collision:MISCONFIGURED]
  “the verdict labels for a safety census: DIRTY meaning a dangerous dep…”
- **hold** [collision:hold]
  “generic sense: a ruling remains in force/valid” | “a board row's lifecycle word claiming that someone is already activel…” | “a row waiting, blocked pending another row's completion” | “a document housing a section or record inside itself” | “to withhold a dispatch until a resource cools”
- **escalation** [collision:escalate collision:escalate-seed]
  “an attention-class category: prose a Builder wrote into a landing tha…” | “a numbered board annotation (E<n>) naming an issue a row's findings r…” | “a noun-form instance: an issue flagged upward for a higher mantle's r…” | “a condition that must stop work and go up the chain, as a classifiabl…” | “a question a row refuses to answer itself and sends up to the owning …”
- **escalated** [collision:escalate(verb) collision:escalate]
  “a session-liveness status token: raised to need Felix's attention” | “surfacing an open question to Felix for ruling, used by Architects/Bu…” | “a DoD/gate item marked as raised to Felix/Architect judgment rather t…”
- **ruled** [collision:ratify collision:bless]
  “Felix making a specific behavioral decision, narrower and more concre…” | “the act of Felix deciding an open helm frame; a batch of such rulings…”
- **brief** [collision:order]
  “to write the work doc that a row will be dispatched against” | “this campaign's uniform name for every row's work doc, regardless of …”
- **fold** [collision:fold]
  “distilling evidence into the documents it changes” | “component name: a reader responsible for surfacing escalations by cau…” | “a queued unit of distillation work, named as a noun” | “the distilled conclusion a sitting produced” | “an app editor-verb name (bound to a key, collapsing/folding a subtree…”
- **FOLD** [collision:fold]
  “a disposition verdict applied when draining an issues inbox: fold the…”
- **red** [collision:green]
  “a failing state on a shared branch — an escalation, never a rewind” | “failing tests on a shared branch” | “a failing-test status token” | “(verb) to make a test/leg fail”
- **census** [collision:census collision:census-vocabulary]
  “the append-only telemetry log of session/hook activity” | “Belvedere's own live account/session-gathering probe mechanism, disti…” | “an exhaustive enumeration commissioned as its own deliverable” | “a dated enumeration of a whole surface, taken so later drift is visib…” | “an enumeration of live sessions across the accounts”
- **glass** [collision:glass]
  “Belvedere, the surface where boards are rendered as cards” | “Belvedere's scope stated as the sovereign's view over all agent work” | “literal greenhouse glass sheltering bred phenotypes — not Belvedere” | “literal greenhouse glass, second occurrence colliding with Belvedere'…” | “the touch/iOS coarse-pointer surface of the app, opposed to the keybo…”
- **ledger** [collision:ledger]
  “to write one's session record into a given ledger file” | “a tracked list of architectural coupling issues found during the spik…”
- **harness** [collision:harness (a test suite, D62/row 13) collision:harness failure]
  “the Claude Code runtime that hosts sessions and enforces presets” | “a browser-driven test apparatus proving a DoD claim” | “the Claude Code runtime whose limits are physics for a session” | “the executable evidence rig a row's DoD is measured against” | “a third-party agent-running tool evaluated against the Guild's needs”
- **dispatch** [collision:dispatch]
  “the mechanism/component that turns a board row into hand commands”
- **flow** [collision:flow]
  “a JSON-defined automation pipeline of nodes (steps) that can be armed…” | “a declared, executable work plan (a DAG of steps) stored as committed…” | “an earlier, more conceptual notion of reactive/continuous dispatching…” | “a generic UX sense — a guided step-by-step wizard for creating a new …” | “a JSON-defined pipeline of steps (a DAG) that the engine can arm and …”
- **contract** [collision:contract]
  “a governing specification a verdict must be checked against” | “the checkable obligations a row must meet to count as LANDED” | “the parts of an order that are the Architect's, not the Builder's” | “the fixed required-field shape an artifact must satisfy” | “the binding terms of a format, stated as a named object”
- **card** [collision:card]
  “a row as rendered in Belvedere's surface” | “a UI element on the rail/City view representing one dispatchable work…” | “a paused flow-engine node specifically requiring Felix's judgment/pas…” | “verb: to present an escalation to Felix as a card, as opposed to staf…” | “a rendered board-row element requiring Felix's own decision, distinct…”
- **sweep** [collision:sweep collision:drain]
  “a uniform mechanical pass applied across many files” | “a long uniform mechanical pass over many items” | “a review pass that clears accumulated venues or inbox entries” | “the Architect's act of processing/clearing the incident inbox” | “to clear and rule on every entry in an inbox in one motion”
- **proven / killed** [collision:KILLED]
  “the binary outcome verdict of a spike: either validated for adoption …”
- **CLOSED** [collision:LANDED collision:CLOSED (retired board synonym, D18)]
  “a banned status token, superseded by LANDED or KILLED” | “a batch-level completion status, distinct from the seeded row-lifecyc…” | “a campaign's DoD met — the same word D18 retired as a board status” | “an open question settled by evidence and needing no further work” | “a whole campaign version finished against its DoD”
- **theater** [collision:theater collision:venue collision:theater (campaign front) collision:campaign]
  “procedural apparatus performed for its own sake — boards, numbering, …” | “the customizable name segment of a fired session's stamp, following t…” | “a naming-lineage exception for mantles (like the Grand Architect) tha…” | “in the glass, the campaign name derived from the selected building ra…” | “the working-directory-derived (or file-listed) campaign name folded i…”
- **blessed** [collision:bless]
  “an order-header metadata field naming who approved the spec for const…” | “the officially endorsed hardware/software configuration chosen for th…”
- **register** [collision:register collision:register (DECISIONS.md) collision:register (metaphor vocabulary) collision:register (DECISIONS) collision:register (linter's buildings) collision:registry]
  “the doctrine-recognized/official record of live city artifacts” | “the in-process cached snapshot of parsed city/board state” | “the glass's cached in-memory parse of the whole city's doctrine and b…” | “the glass's canonical directory-walking subsystem that decides which …” | “the doctrine rule set defining which directory constitutes a 'buildin…”
- **building** [collision:house collision:city]
  “one repo or project, as a structure in the city” | “a repo/directory-level unit within the city that the glass renders” | “a project repo, in the city register, as a routing destination” | “any directory carrying the Guild's record files — the census unit of …” | “one repo/project in the city”
- **d#** [collision:D-entry]
  “a shorthand reference to a ratified project decision, numbered indepe…”
- **spike** [collision:lab]
  “a small bounded experimental project or investigation” | “a disposable experiment run to confirm a mechanism” | “a bounded empirical probe run to settle a mechanism question” | “a short exploratory experiment run to test a question” | “a bounded, disposable exploratory-build row genre, distinct from a br…”
- **chrome** [collision:chrome]
  “a coverage-key class for UI behaviors/tints/toggles that are not tied…” | “the console's own UI shell/visual styling — a distinct sense from the…” | “shorthand for the product's own on-screen button/label text, and by e…”
- **home** [collision:venue]
  “to be the single place a fact lives” | “the one canonical file a piece of knowledge is allowed to live in” | “the directory a campaign lives in and writes to” | “the single canonical location where related logic must live, so it is…”
- **PENDING** [collision:PENDING]
  “labels an entire phase's terminal human ruling, not an OPEN board row…” | “the minting site: PENDING applied to an external human ruling the tex…”
- **pending** [collision:PENDING]
  “a verb-outcome token for a chord whose handling verb does not exist y…”
- **rig** [collision:venue]
  “an assembled apparatus for running an experiment” | “a scratch apparatus (scripts) built to test a mechanism” | “the pre-existing cmux/summon tooling layer Felix already runs (colour…” | “the keystroke launcher that fires mantled sessions” | “the summon.zsh tooling that composes and fires Claude Code launch com…”
- **leg** [collision:row]
  “one of two independently-run conditions/attempts within a single drill” | “one distinct sub-portion of an order's work items, treated as separat…” | “a bounded, separately-conditioned portion of a deployment order (tail…” | “a single named step/segment within a drill or bench run, treated as i…”
- **title** [collision:mantle]
  “a project-local role name predating canon mantle adoption”
- **budgets** [collision:gauge]
  “named performance targets a mechanism must satisfy, revisited as meas…” | “a table of named performance ceilings (append, checkpoint, load, open…”
- **city** [collision:city]
  “proper noun for Belvedere's top-level dashboard view, distinct from '…”
- **cap** [collision:ceiling]
  “the ceiling on concurrent cells”
- **arms** [collision:work]
  “a card 'arms' when it carries live fire wiring; here measured across …” | “this row's section header for its numbered task list — the same conce…”
- **dead** [collision:KILLED]
  “a verdict token marking a hypothesis ruled out by evidence, in a tabl…”
- **DEAD** [collision:VIABLE]
  “a technical option ruled non-viable by evidence, distinct from a boar…” | “a status marking a feature/control as removed from the product entire…”
- **deck** [collision:deck]
  “Belvedere's unified single-page app replacing the v0 multi-page UI” | “redefined mid-campaign: 'the deck' stops meaning the v0 card surface …”
- **rider** [collision:rider collision:rider (dispatch appendix) collision:rider(canon:dispatch-appendix)]
  “a parenthetical annotation in a board cell, for eyes only” | “a mandatory attached condition on a permission — a third sense of rid…” | “fixed appendix text auto-appended to every Belvedere dispatch kickoff” | “a follow-up constraint or consideration from one row's landing that a…” | “a secondary consideration attached to a finding, flagged for a future…”
- **riders** [collision:rider]
  “small, sanctioned side-fixes bundled into a row's branch alongside it…”
- **host** [collision:venue]
  “the machine or account carrying the load of parallel cells” | “the machine used to run this session's work, synonymous here with 've…”
- **stamp** [collision:stamp]
  “the generated identifier label (mantle-theater-ordinal) assigned to a…” | “the auto-generated session identifier (mantle-building-ordinal) assig…” | “the original fired-session name-stamp, redefined as the 'birth name' …” | “a generated lineage identifier stamped onto a fire (mantle-theater-or…” | “to formally apply/ratify a marker onto a decision”
- **closed** [collision:LANDED]
  “a cmux workspace that has been shut, whose reference now fails rather…” | “the completion state token for a batch or version, sibling to LANDED …” | “a specific candidate option within a spec question has been ruled out…” | “marking an escalation/open question as fully resolved” | “a batch of dispatched rows has finished, all landed/killed with nothi…”
- **slot** [collision:slot]
  “a named gap in a canon template that project doctrine fills” | “one unit of the concurrency ceiling, freed when an agent finishes” | “a named blank in a template that each project fills” | “a named field position an artifact's grammar requires be filled” | “a pane region that can hold one of several interchangeable views”
- **escalate** [collision:escalate]
  “to route an inbox entry to the appropriate decision venue by category…” | “a Builder (not only a Dispatcher) raising a blocking issue to the Arc…”
- **fork** [collision:fork]
  “a decision point a running session can hit — must be pre-decided or n…” | “a baton whose move is Felix choosing between instrumented exclusive o…” | “reality diverging from what the work doc anticipated” | “a baton shape offering two exclusive next-step options, one named as …” | “an unresolved decision branch blocking a row until it is briefed”
- **GENESIS** [collision:master-doc]
  “the retired name of the master architecture doc, now MAP.md” | “whiteboardy's own founding/master document, read at the start of ever…”
- **sitting** [collision:sitting collision:session collision:bee]
  “one Architect review session, treated as a recurring formal occasion” | “one continuous working session, counted as an occasion” | “the Architect's recurring review occasion” | “a recurring work session at which the Architect processes the inbox” | “a named, purpose-specific continuous work engagement by one mantle-ho…”
- **passed** [collision:PASSED]
  “a lowercase, informal harness test-run success verdict, distinct from…”
- **e-number** [collision:U-number]
  “a numbered open question a Builder/Digger raises in a landing report …” | “a sequential identifier for an open engineering unknown within a brie…”
- **F2** [collision:D-entry]
  “a named proposal/finding number, distinct from a ratified D-entry, tr…”
- **countersign** [collision:countersign]
  “Felix's confirmation converting a proposal into ratified law” | “an attention-class category: a decision proposed but not yet counters…” | “same dispute site: whether countersign and bless remain two distinct …” | “the confirming signature/token Felix gives a proposed D-entry, as a n…” | “an Architect (not Felix) affirming or overturning a Builder's narrowe…”
- **blessing** [collision:bless]
  “Felix's approval of a spec for construction, as a noun” | “noun form of the seed verb 'bless': the state or process of being ble…” | “Felix's approval message treated as a measurable artifact with a leng…” | “a noun use naming the specific approval act — the seed gloss only cov…” | “the session/moment at which Felix approves a spec, treated as a named…”
- **amendment** [collision:addendum]
  “a mid-flight addition to a running batch, binding like the original” | “a proposed change entered against an existing D-entry or spec” | “a formal modification appended to an existing D-entry” | “a formal, dated correction issued at a later gate that revises a prio…” | “a proposed change to a standing spec section, raised for a gate to ru…”
- **chain** [collision:batch]
  “a serial run of dependent rows tended as one unit” | “a strictly sequential run of rows, each waiting on the last” | “an ordered sequence of rows dispatched one at a time within a batch, …” | “a batch's rows ordered as a strict one-agent-at-a-time execution sequ…” | “the ongoing serial dispatch sequence, which proceeds independently of…”
- **wall** [collision:wall]
  “the architectural invariant restricting native text selection/editing…” | “reuse of the single-editable-row architectural invariant, elsewhere n…” | “a hard constraint imposed by a specific D-entry ruling, treated as a …” | “an environmental obstacle blocking a dispatched session (e.g. a locke…” | “a hard, named standing constraint from a prior decision (D16) that la…”
- **tick** [collision:the tick (night-shift.md) collision:tick (rig)]
  “the engine's discrete time-step/pulse unit” | “a timed repaint/check cycle inside the panel, occurring without user …” | “the hourly autonomous routing/dispatch cycle of the proposed night-sh…” | “one engine pass that reads all relevant state, computes each step's r…” | “the cron-triggered automated deploy invocation, spoken of as a proper…”
- **hook** [collision:hook collision:hook (PreToolUse harness hook)]
  “a written obligation left for a future numbered row to discharge” | “a harness-executed automation, named as the other place a binding law…” | “a pointer clause planted in one doc so another doc's law reaches read…” | “a clause deliberately planted in one doc to bind it to a law defined …” | “a harness-level interceptor enforcing dispatch rules”
- **rail** [collision:board]
  “the automated pipeline that turns a board row's work doc into hand co…” | “a UI sidebar component displaying baton cards” | “Belvedere's surface that renders a baton as pressable actions” | “a v0 UI surface listing dispatchable/fireable items, whose content is…” | “the home page (`/`) rendering every baton in the city as one column o…”
- **HELD** [collision:BLOCKED]
  “a proof clock/timer paused while the page is hidden, so occlusion tim…” | “a verdict token for a hypothesis confirmed true by evidence, paired w…” | “a row paused awaiting a specific named decision, distinct from BLOCKE…” | “the paused state of a countdown timer while its precondition (visibil…”
- **inbox** [collision:inbox]
  “a per-campaign README subsection holding entries lodged for that camp…”
- **felix-gate** [collision:Felix-gate]
  “a gate row only Felix can pass — the chain stops there” | “a formalized ledger/board notation tag standardizing how a Felix-only…” | “same concept as the seed's hyphenated Felix-gate — a gate that is rea…” | “adjectival form of Felix-gate — a step requiring Felix's gated approv…”
- **bare mode** [collision:bare-launch]
  “launching a tier with no charter attached” | “firing the rig with no mantle preset selected, same concept as 'bare …”
- **wave** [collision:wave]
  “a baton handing n instruments the holder could all fire now” | “a group of rows dispatched simultaneously within the concurrency ceil…” | “one of the three baton kinds (move / wave / fork)” | “a grouped set of ready-to-fire dispatches presented to Felix to launc…” | “verb: to let something through a checkpoint without stopping it (a pe…”
- **candidate** [collision:fold candidate]
  “one numbered, diagnosed correctness-debt item queued for a hygiene row” | “a numbered, named-but-not-yet-cut pending fix tracked so it stays 'tr…”
- **blocked** [collision:BLOCKED(board)]
  “a session-liveness status token distinct from the board-row BLOCKED l…”
- **d#/th-d#** [collision:D-entry]
  “bob's local decision-numbering scheme: a bare D# for a proposed choic…”
- **wedge ledger** [collision:wedge-watch]
  “a log of unforeseen permission prompts encountered by autonomous work…”
- **cold** [collision:cold session]
  “a trust status: a repository with no Claude Code project-trust entry …”
- **act** [collision:act]
  “a numbered subdivision within one continuous ledger entry/sitting mar…” | “a single deliberate action (e.g. firing a session) that simultaneousl…” | “a numbered continuation of one sitting inside one window”
- **tile** [collision:card]
  “one pod's rendered card on the Order View wall-board” | “one pod's rendered display unit on the rail — a card-like element”
- **desk** [collision:desk collision:the desk collision:office]
  “the office an item is routed to for judgment” | “the (not-yet-built) home for Felix's per-viewer working files, gitted…” | “D17's built feature: a flat, frontmatter-free notes drawer plus compo…” | “idiom for the current sitting's (or Felix's) own remaining scope of r…” | “the Architect's own holding place for rows between states”
- **mint** [collision:mint]
  “creating a canonical artifact for the first time” | “create a disposable scratch project directory for a probe” | “the product's own act of allocating a UR runtime variable — unrelated…” | “to allocate/create a new UR-controller variable on the robot from an …”
- **warm** [collision:cold]
  “a session still holding its full context, so returning to it costs no…” | “a JVM/system that has completed warm-up, as opposed to cold/first-exe…”
- **console** [collision:console]
  “the compiled-preview command that renders authored topics as reader-f…”
- **escalations** [collision:escalation]
  “a documented category of filed items awaiting a Felix/Architect rulin…” | “here escalations are recorded as a plain numbered list without the ca…”
- **PROPOSED** [collision:OPEN]
  “a board-row status for a row drafted but not yet ratified/cut into th…”
- **stack** [collision:arc]
  “a cumulative set of layout changes/commits treated as one accounting …”
- **charter** [collision:mantle]
  “the written file that constitutes a mantle” | “the mantle document a session reads at wearing” | “the document embodying a mantle's role definition”
- **STOPPED** [collision:HELD]
  “an active mid-run halt triggered by a discovered false assumption or …”
- **envelope** [collision:envelope]
  “the bounded scope of previously-approved design/shape within which an…” | “the bounded scope a spec/order is pre-authorized to act within” | “the scope boundary of a blessed batch — what work is pre-approved ins…” | “the scope boundary declared for a row, stating exactly which files/ar…” | “the declared scope boundary of authorized work for an order, stated a…”
- **RESOLVED** [collision:CLOSED]
  “a spec question/unknown has been settled with evidence and a ruling m…”
- **teardown** [collision:teardown (deleting a venue, D55)]
  “the deletion of a disposable venue at landing” | “an argument dismantling one's own proposal” | “an office dismantling its own proposal to test whether it survives” | “the sovereign's hostile review of a finished package before signing” | “the cost of closing/detaching the previously displayed view when a ne…”
- **harvest** [collision:harvest]
  “a sitting whose business is promoting proven patterns into canon” | “generalizes 'harvest' (promote a proven pattern into canon) to descri…” | “extracting and reusing legacy-manual content into the new manual unde…” | “extracting/reusing legacy-manual content under Tier-2 discipline — sa…” | “borrowing/adapting visual inspiration from a reference design, bounde…”
- **acceptance** [collision:Definition of done]
  “a brief's own section naming the pass/fail criteria for a single row”
- **re-cut** [collision:cut]
  “to rewrite a row and its work doc after it blocked” | “to revise/reopen an already-cut work item, adding new scope” | “to reform an existing document into a new grammar without changing it…” | “to re-form existing documents into a new shape without changing meani…” | “to redo/reformat an artifact (like a board's columns) into the curren…”
- **matured template** [collision:mint]
  “the process of creating a golden-state template by booting once and s…”
- **helm** [collision:inbox]
  “the standing Felix-facing queue of open decisions a campaign files ru…” | “Felix's personal blocked-on-him queue, gitignored and machine-local, …” | “apparently a tool/channel through which Felix issues rulings into thi…” | “the routing destination/queue for blockers and asks that only Felix c…” | “a Felix-facing escalation/ruling queue for flagged (⚑) issues, distin…”
- **retire** [collision:strike]
  “to end a term's old usage in favour of the promoted one”
- **M4** [collision:M4]
  “here 'M4' names the specific authoring rule governing which UI labels…”
- **initial** [collision:dream]
  “Felix's raw, unedited founding request for a campaign — the pre-D33 n…”
- **D9** [collision:D-entry]
  “the numbered set of five durability drills required for this milestone”
- **hole** [collision:my best work is a hole]
  “a gap in the mantle set or in enforcement, proven by an incident” | “a known uncloseable gap in a guard, deliberately documented and watch…” | “a known, documented gap in a landed thing's coverage” | “an identified gap in verification coverage recorded for future closur…”
- **certificate** [collision:verdict]
  “a formal record that a manual verification pass was completed cleanly…” | “a formal record that Felix's hand-run of a milestone's manual check p…” | “a short scripted set of pass/fail checks run by hand on a real device…” | “a manual on-page verification checklist/run that stands as evidence a…” | “a staged, thumb-run drill document that stands as formal proof a surf…”
- **holder** [collision:holder (of a baton)]
  “the single party whose move comes next after a session ends” | “the structural rule determining which entity legally possesses a bato…” | “the baton field naming who currently owns a row's next action (sessio…” | “the structured-answer field naming who currently holds the baton afte…” | “the role currently holding responsibility for an in-flight baton”
- **a-number** [collision:D-entry]
  “this campaign's own lettered, numbered decision-ledger entries (A1…A2…”
- **drain_dialogs** [collision:drain]
  “repeatedly dismiss GUI dialog boxes during boot until none remain — a…”
- **ladder** [collision:ladder]
  “an ordered sequence of design options investigated from most to least…” | “the ordered precedence chain a field's value resolves through (MANUAL…” | “a bounded sequence of retry attempts with increasing delay” | “a graduated sequence of step values (glow intensity, column count, ch…”
- **ceiling** [collision:ceiling]
  “the highest effort a given model actually supports” | “the maximum number of rows allowed to run at once” | “the maximum number of agents allowed running at once” | “the maximum number of sessions a batch may run at once” | “a measured maximum sustainable throughput implied by a per-operation …”
- **physics** [collision:project physics]
  “the invariant constraints of a thing, as against its policies” | “the unchangeable facts of an environment, as opposed to its law” | “the unchangeable constraints of a venue that no doctrine can override” | “the governing rules of how a voice file grows” | “the fundamental, non-negotiable constraints or feel-benchmarks of a s…”
- **shelf** [collision:the shelf]
  “the accumulating list of one-line lessons each seat adds to the Log” | “the Grand Architect's Log shelf, cited as shared doctrine from anothe…” | “the UI listing of all resumable past sessions across accounts, source…” | “the glass feature listing and resuming any past session, any account” | “a v0 UI surface for session lists/resume, dissolved into Workshop ses…”
- **lane** [collision:lane]
  “the sanctioned channel a kind of item travels by” | “one parallel workstream within a dispatched batch of rows” | “one of several parallel dispatch tracks within a batch, each with its…” | “inside the flow engine, a linear execution sequence of steps within a…” | “the clarification route that avoids spending a new decision number”
- **G1** [collision:G1(row10-press-gate)]
  “the final Architect-run gate ruling on the campaign's merge — a diffe…”
- **drain** [collision:drain]
  “the product's own flush order for a queued burst of writes — a techni…” | “emptying the event-dispatch-thread's pending task queue — a technical…” | “repeatedly dismissing a sequence of blocking UI dialogs until none re…”
- **skipped-named** [collision:PENDING-named]
  “explicitly marked SKIPPED with the reason given, rather than silently…”
- **slots** [collision:slot]
  “the canon rider-template's fill-in-the-blank fields, completed when a…”
- **outcome** [collision:findings]
  “this row's landing-report header, used in place of the more common 'F…”
- **lineage** [collision:lineage (rig ordinal) collision:lineage (D38 ancestry)]
  “the full history of sessions ever fired under one mantle-theater name…” | “a traceable line of descent through renames and successor sessions” | “the fictional ancestry canon declares itself descended from” | “a series of sessions sharing a name prefix, numbered in sequence” | “the ordered succession of seats holding one office”
- **twin** [collision:twin-worktree]
  “a parallel copy of the corpus in the alternative form, built to be co…” | “a parallel structured-JSON representation of a building's truth, buil…”
- **code** [collision:code (source code)]
  “canon seen as a building code — rules every builder builds under”
- **fenced** [collision:fence (scope boundary) collision:the fence]
  “wrapped in a markdown code fence so it can be copied byte-exact” | “a course of action ruled out as against methodology — here, guessing …”
- **voice** [collision:canon voice]
  “a register of writing that carries no authority” | “proper name for the route delivering Felix's typed words into a sessi…” | “the Chat feature, framed as the organ giving Belvedere two-way commun…”
- **PAID** [collision:Felix-gate]
  “a Felix-gate whose condition has been met, unblocking its row” | “the state of a Felix-gate once its precondition has been satisfied an…”
- **house** [collision:building]
  “one repo/project as a dwelling with its own laws”
- **picker** [collision:panel]
  “a knob-selection control (e.g. mantle picker, account picker)” | “the interactive panel/selector widget (same as 'panel')”
- **dig** [collision:Digger]
  “the Digger's numbered working procedure” | “the waggle's fourth line, pointing at where to verify” | “to go verify a compressed signal at full depth” | “the fourth slot of a waggle, which may fall silent when the depth is …” | “a specific, pointed investigative question posed by Felix (via a wagg…”
- **parallel-safe** [collision:parallel-affordable]
  “rows that cannot corrupt each other's files if run at once” | “a row's property of being runnable alongside others — disputed here a…” | “row marker permitting simultaneous dispatch” | “the judgment that parallel rows will not collide on files/docs”
- **UNVERIFIED** [collision:unrecorded]
  “a named evidentiary question flagged as not yet confirmed from a prim…” | “an all-caps state token marking a load-bearing claim with no closing …” | “reuse instance, showing an UNVERIFIED tag being resolved ('dies') by …”
- **type gate** [collision:gate (a judgment row)]
  “the named check a row's deliverable must pass, by class”
- **silo** [collision:silo (account isolation)]
  “a project's own bounded namespace of sessions and history”
- **OWED** [collision:owed]
  “a capitalized status label for bounded follow-up work a landed row na…”
- **field report** [collision:findings]
  “an observation filed mid-work from wherever it was noticed” | “an incident/observation entry filed to the inbox for later ruling” | “Felix's informal, numbered batch of observations/complaints delivered…” | “an informal ground-level observation filed for later triage, distinct…” | “an incident observation filed from live work”
- **G3** [collision:G1(FG1-campaign-gate)]
  “the third press-time refusal check, added by this row for the safety/…”
- **lease** [collision:claim]
  “a shard's temporary hold on one instance, distinct in scope from a cl…”
- **capability tier** [collision:tier]
  “the model×effort preset half of a session's identity” | “an agent definition that sets model and effort and nothing else” | “the model×effort preset, in its full ceremonial name”
- **reach ladder** [collision:ladder]
  “a staged sequence of increasingly strict network-connectivity tests” | “an ordered fallback sequence of connectivity attempts, part of a recu…” | “a sequence of network-address attempts tried in order to determine ho…”
- **felix-card** [collision:card]
  “a rail card whose action requires Felix's own hand, at which the flow…”
- **gate a** [collision:gate]
  “a named, dated Felix-ruling checkpoint that packages budgets/prioriti…” | “a named phase-boundary Felix-ruling checkpoint that unblocks the next…” | “a named decision checkpoint where Felix rules on budgets/priorities b…”
- **implemented** [collision:LANDED]
  “a fix-record status token meaning the change has landed in a commit, …”
- **waggle** [collision:waggle]
  “the fixed request phrase that invokes the four-line decision-density …” | “the four-line whole-field signal, served on request from any mantle” | “the four-line compressed decision signal, at its ratification site” | “a loose, informal confirmation exchange with Felix — not the formal f…” | “extended here beyond canon's four-line decision signal to mean the na…”
- **encapsulation** [collision:encapsulation-first]
  “the short derived/authored name standing in for a longer record (a ro…” | “a short (≤6 word) name/label summarizing a rail card or row for displ…” | “the design law that a thing's minimal-state label must be reduced to …” | “a proposed doctrine field: the 1-to-6-word canonical name for a row/t…”
- **decoration** [collision:load-bearing]
  “a proposed token that changes no session's behavior”
- **instantiated** [collision:mint]
  “created (this rider) from the canon template at campaign founding — l…”
- **permission classifier** [collision:permission guard]
  “the harness component that decides whether an agent's write needs Fel…”
- **fixture city** [collision:city]
  “a fake city directory built purely to exercise the rail/glass on cont…” | “a synthetic mock version of 'the City' view used for reproducible tes…”
- **pokes** [collision:baton]
  “a lightweight inter-session message carrying a handoff, distinct from…”
- **hand item** [collision:Felix touch]
  “a specific piece of execution mechanics that only Felix can perform, …” | “a task/step that requires Felix's own manual action rather than autom…”
- **unresolved** [collision:unrecorded]
  “typed-absence token for a decoder reference that cannot be resolved w…”
- **an order** [collision:Guild]
  “Felix's original name in the founding letter for the collective/movem…”
- **judgment calls** [collision:ratify]
  “decisions a Builder makes autonomously mid-order that need after-the-…”
- **parked adjacent** [collision:adjacent]
  “a neighboring improvement noticed during a row and deliberately left …” | “an out-of-scope improvement identified but deliberately deferred, rec…” | “a discovery noted as out of scope for the current milestone but flagg…”
- **files carry truth** [collision:files carry the truth]
  “the three-part structure that makes the sovereign's blessing cheap” | “variant wording of the canon maxim justifying re-reading the plan fil…”
- **unowned** [collision:unstaffed]
  “typed-absence marker for a registry row with no attributable owner”
- **scratchpad** [collision:lab]
  “a location for a disposable one-off verification script, not part of …”
- **felix-run** [collision:Felix-gate]
  “marked as executable only by Felix's own hand, never an agent's” | “a step no agent may execute — reserved to the sovereign's own hands” | “a step that must be executed by Felix personally, never delegated to …”
- **PD-D** [collision:D-entry]
  “campaign-prefixed D-entry numbering, distinct per campaign, rather th…”
- **sign-off** [collision:countersign]
  “Felix's approval of a canon change, materialized as a D-entry”
- **flow chapter** [collision:chapter]
  “a discrete phase/deliverable of Belvedere's flow-engine work, structu…”
- **contract doc** [collision:contract]
  “a durable doc that fixes a system's guaranteed behaviour”
- **lb-d#** [collision:D-entry]
  “campaign-scoped, campaign-prefixed decision-entry numbering local to …”
- **pd-d#** [collision:D-entry]
  “campaign-scoped, campaign-prefixed decision-entry numbering local to …”
- **permission guard** [collision:permission classifier]
  “the harness gate that refuses an agent's dangerous action — used by d…” | “the harness's approval prompt, used deliberately as a control” | “the harness barrier whose tripping is the designed signal that a step…”
- **auto-fire** [collision:fire]
  “the glass dispatching a row without Felix pressing anything”
- **ground truth** [collision:the two exceptions]
  “facts about the world only the sovereign holds” | “the authoritative correct answer set a session's output is checked ag…”
- **unstamped** [collision:unrecorded]
  “a session transcript that carries no agent-name record at all, render…”
- **buried** [collision:strike]
  “retiring a superseded document by deleting it and preserving it only …”
- **landing gate** [collision:gate]
  “the collective set of proof obligations required before a change is m…” | “row 03's role as the final verify-then-cutover checkpoint before merg…”
- **w-<n>/m-<n>** [collision:M4]
  “identifies one specific walk-log MISMATCH row — a third distinct numb…”
- **mantle row** [collision:row]
  “the rig's row of mantle-selection keys”
- **comb** [collision:comb]
  “redefinition of comb from 'mere memory' to durable inherited structure” | “to go through open windows extracting what is worth keeping”
- **altitude** [collision:flavor altitude]
  “the level of a document a change is allowed to touch — voice, name, o…” | “the level of abstraction a session is working at”
- **diary** [collision:Log]
  “the Log/SAPHO files under their informal name”
- **gate b** [collision:gate]
  “a named phase-boundary Felix-ruling checkpoint, the chapter-2 counter…” | “a named decision checkpoint where the Architect/Felix rules on a gene…”
- **glass-shatters** [collision:glass]
  “the design test that if a dependency (like the glass) dies, nothing e…”
- **suite debt** [collision:unrecorded]
  “a named, tracked liability of un-run or unverified test suites owed a…”
- **un-park trigger** [collision:park]
  “the specific observed condition that converts a previously-parked iss…” | “a documented condition that, once satisfied, converts a previously de…”
- **landing note** [collision:findings]
  “this row's landing-report heading — a third variant name (alongside '…” | “a Findings subsection recording post-landing follow-up steps a merge …”
- **c-d#** [collision:D-entry]
  “a campaign-scoped, campaign-prefixed decision-entry numbering local t…”
- **keel-note** [collision:keel]
  “a draft toward a campaign's founding plan” | “a preliminary planning document preceding the full keel” | “a keel-grade deliberation recorded without founding a campaign yet” | “a filed pre-keel note capturing a campaign not yet founded” | “a deliberative planning document that precedes a formal keel (foundin…”
- **cold hands** [collision:hands]
  “the disarmed state of the hands mechanism, which must still permit dr…”
- **off-register** [collision:register]
  “describes content, panels or sessions that fall outside the tracked/f…” | “a directory the register's rule legitimately excludes” | “a building sitting outside the expected repo, recorded as fact rather…” | “a building/session outside the canonical register that the glass stil…”
- **sovereign's inbox** [collision:inbox]
  “the queue of things awaiting Felix's own attention, as a first-class …” | “a channel by which Felix's free-text notes and board gestures reach t…”
- **uniform** [collision:uniform]
  “the office's color as a checked identity mark”
- **auto-bless** [collision:bless]
  “a pre-ruling letting orders inside a countersigned envelope dispatch …” | “a standing rule letting certain orders proceed without a live Felix b…” | “dispatch proceeds without requiring Felix's explicit sign-off first”
- **spine** [collision:the spine(build-order sense)]
  “the minimal foundational build order (census, batons, glass) prioriti…” | “proper name for the glass server itself as the structural backbone of…”
- **cmux is truth** [collision:files carry the truth]
  “the ruling that cmux's own live socket state, not any cached copy, is…”
- **truth pass** [collision:true]
  “a content pass dedicated to truing (correcting) recorded data against…” | “a named genre of campaign work: auditing documentation against code t…”
- **VIABLE** [collision:DEAD]
  “a technical path confirmed workable by measured evidence”
- **as-if-done** [collision:D10]
  “the drafting convention (ruled as decision D10) of writing a chapter …” | “the same as-if-done convention cited elsewhere as decision 'D10' is h…”
- **naming law** [collision:Naming law (D25)]
  “the rule fixing tier filenames to their frontmatter” | “the rule assigning file-name casing by artifact role” | “D25's rule for how canon files are named by their role”
- **felix-action** [collision:Felix-gate]
  “a next move that is Felix's own hand — a countersign, a smoke, a ruli…” | “the baton's instrument when the next act belongs to Felix”
- **bench pass** [collision:bench day]
  “an informal session where Felix personally tests on a real bench setu…”
- **spike code** [collision:lab]
  “throwaway/exploratory code contrasted with product code”
- **required slot** [collision:slot]
  “a field a record grammar obliges to be filled”
- **helm-gated** [collision:felix-gate]
  “a fact whose printed content depends on an outstanding helm ruling, h…”
- **TH-D4** [collision:D-entry]
  “a theseus-campaign-scoped decision ID, numbered separately from the G…”
- **keel sitting** [collision:keel collision:sitting]
  “the session that lays a campaign's founding plan” | “a founding/foundational working session (sitting) that lays out a new…”
- **single-writer physics** [collision:project physics]
  “the constraint that a shared checkout tolerates only one writer/row i…” | “the rule that steps sharing a plain checkout must run strictly serial…”
- **parse, don't validate** [collision:CLAUDE.md directive]
  “engineering maxim (echoing Felix's own coding directives) applied her…”
- **glassdoors** [collision:the door]
  “the named registry of guarded verb-dispatch doors (run/bar/menu/drag)…”
- **relay test** [collision:relay]
  “the check that a baton's next move already exists as file text” | “the check that a handed move already exists as copyable text” | “the check that a baton can actually be handed on as written”
- **flavor altitude** [collision:altitude]
  “the doc layer where metaphor is allowed — epigraphs and framing, neve…” | “the permitted level at which metaphor may enter canon — wording, not …”
- **staffing rider** [collision:rider]
  “a parenthetical qualifier in a Staffing cell”
- **live wire** [collision:the wire]
  “a channel able to carry messages between siloed accounts”
- **origin vision** [collision:dream]
  “hexwright's descriptive name for the file the seed list glosses as 'd…”
- **governing contract** [collision:contract]
  “the specific doc section that decides a behaviour question” | “the spec document a system's behavior is judged against”
- **re-fold** [collision:fold]
  “redistilling a standing summary once its tail has taught it something” | “to rewrite the folded head from the accumulated entries”
- **kill shot** [collision:kill criterion]
  “a self-evident piece of evidence so damning it settles an argument ou…”
- **clean break** [collision:cold start]
  “ending a session at a boundary rather than continuing it warm”
- **keep it fun** [collision:Keep the joy]
  “the Mentat's sign-off benediction, dialect of the Log's Keep the joy”
- **project silo** [collision:silo]
  “the isolated bundle of history, resume state, and auto-memory keyed t…”
- **waggle-dig** [collision:waggle]
  “a single pre-registered yes/no question posed to Felix as a merge-blo…” | “a compound term naming a Felix-posed investigative question delivered…”
- **off-charter** [collision:mantle]
  “acting outside the authority granted by one's kickoff/mantle scope”
- **pd-d18 amendment** [collision:D-entry]
  “a live-feedback ruling that formally amends a previously ratified dec…”
- **ready to build** [collision:OPEN]
  “a fix-record status meaning a diagnosed follow-on fix is specified an…”
- **pain ledger** [collision:ledger]
  “a dedicated ledger for recording operational friction/pain points” | “a running record of felt friction, kept as the evidence a future mint…” | “a running log of felt operational pain points, appended over time as …”
- **master architecture** [collision:master doc]
  “MAP.md's role as the project's top-level structural doc”
- **furniture** [collision:harness furniture]
  “the non-data chrome of a rendered surface”
- **wedge-watch** [collision:wedge (night-shift.md)]
  “a recurring check cadence for monitoring a shared resource's health d…”
- **keel recon** [collision:keel]
  “a pre-session reconnaissance pass establishing project state before a…”
- **christening** [collision:mint]
  “a local synonym for a naming-mint event — several artifacts christene…”
- **harvest window** [collision:harvest]
  “a session-type named by combining the seed verb "harvest" with "windo…”
- **false fail** [collision:false red]
  “a test's FAIL verdict that is guaranteed and meaningless by construct…”
- **a procedure nobody walked is fiction** [collision:M4]
  “the founding principle, dubbed 'the M4 law', that no drafted procedur…”
- **venue-blocked** [collision:BLOCKED]
  “a row status meaning it cannot proceed because no execution environme…”
- **session contract** [collision:contract]
  “the start-and-end obligations binding every session, any mantle”
- **byline move** [collision:carrier]
  “handing authorship to the person you need to carry the plan”
- **cold-boot check** [collision:start cold]
  “verifying a project's doc set answers standard cold-start orientation…”
- **cold-boot walk** [collision:cold-boot check]
  “a narrated dry run through summon/dispatch mechanics without actually…”
- **staging retired** [collision:staged]
  “the v1 panel design where a key's meaning depended on selection stage…”
- **attendance rider** [collision:rider]
  “a parenthetical note in a board row's Staffing cell recording who att…”
- **candidate residue** [collision:residue]
  “terms surfaced by mechanical heuristics (typography, frequency) that …”
- **window, not a workbench** [collision:window]
  “design principle: Belvedere only observes/displays the city's truth, …”
- **mentat-02 sitting** [collision:Mentat]
  “a named session/sitting stamp tying back to Felix's Mentat collaborat…”
- **cold arm** [collision:warm]
  “a measurement branch taken immediately after a fresh boot/reload, bef…”
- **ride red** [collision:park]
  “a known-failing budget deliberately left failing under a named ruling…”
- **fail fast, fail loud** [collision:coding-directive]
  “cited as 'global law' — surfacing errors immediately and visibly rath…”
- **m1…m12** [collision:M4]
  “the numbered-rule-id scheme for the book's prose linter, sharing the …”
- **precedence clause** [collision:the precedence law]
  “the standing rule for which law wins when charter and global directiv…”
- **complete contract** [collision:contract]
  “a charter that needs no other document to be followed”
- **usage decides the fold** [collision:fold]
  “whether a practice enters canon is settled by how much it gets used”
- **---** [collision:——]
  “horizontal rule separating entries that carry evidence blocks” | “horizontal rule delimiting an evidence block around an inbox entry” | “the horizontal rule that delimits one ledger entry from the next — lo…” | “a markdown horizontal-rule used to fence off a multi-line inbox entry…”
- **blessing = this countersign** [collision:bless]
  “one signature act discharging both the bless and countersign rituals”
- **register density** [collision:register (metaphor vocabulary)]
  “the signal-per-entry of DECISIONS.md, treated as a resource to protect”
- **laboratory** [collision:lab]
  “every non-canon repo, where patterns are proven before harvest”
- **saturation incident** [collision:load 328]
  “the named parallel-agent overload failure that begat zoning”
- **third grand architect** [collision:grand-architect-02]
  “one-indexed ordinal address for a seat, colliding with zero-indexed i…”
- **i don't edit the window i was** [collision:I don't edit the person I was]
  “the Log's person-formula restated in the Mentat's window-terms”
- **commitment gate** [collision:gate]
  “a business checkpoint making an invalid commitment unrepresentable”
- **guard-armable** [collision:arm]
  “a risk that a mechanical guard can be switched on to cover”
- **handoff arrives already wearing their uniform** [collision:uniform]
  “a deliverable styled as the recipient's own document”
- **to-do card** [collision:card]
  “a rail card representing a baton held by Felix, requiring his manual …”
- **eye-verdict** [collision:verdict]
  “an informal visual-inspection judgment (Law 5) — not one of the board…”
- **fork in the road** [collision:fork]
  “a decision point where reading a specific piece of evidence first det…”
- **decision log** [collision:ledger]
  “a per-doc running record of ratified rulings, functionally parallel t…”
- **harness failure** [collision:harness]
  “a failed run caused by the test-running tooling itself (Xcode test ha…”
- **free finding** [collision:adjacent discovery]
  “an unplanned, incidental discovery encountered while doing other work…”
- **wrong-code-looks-right** [collision:coding-directive]
  “cited as a standing codebase principle against naming/spelling choice…”
- **canon defect filed** [collision:issues]
  “a formal report of a flaw discovered in the Guild's own canon (mantle…”
- **ruled go** [collision:bless]
  “a terse one-word affirmative ruling authorizing a proposed change mid…”
- **warm window** [collision:warm reading]
  “a session still open and holding its own context”
- **my best work is often a summons i never run** [collision:my best work is a hole]
  “the Mentat's output is a dispatch handed to someone else”
- **LANDED — MERGED** [collision:MERGED]
  “a compound board-status token combining the board lifecycle state LAN…”
- **rulings (pre-chewed)** [collision:pre-chew]
  “a section heading operationalizing the seed 'pre-chew' concept with a…”

## polysemy leaderboard (≥3 distinct glosses)

- **gate** ×86: “to be the judgment a following row must wait behi…” | “a judgment step standing between two rows” | “an automated pass/fail typecheck barrier (bunx ts…” | “an automated typecheck pass/fail barrier” | “the automated offline typecheck pass/fail barrier” | “the offline TypeScript typecheck run, treated as …”
- **verdict** ×58: “a conclusion the rest of the campaign will inherit” | “an authoritative statement about how the built sy…” | “PASSED / MERGED / BLESSED — the outcome token of …” | “a conclusion drawn from measurements” | “same object as the Felix-card: the specific pause…” | “a chapter/batch-level yes-or-no ruling on whether…”
- **window** ×52: “one Mentat sitting, framed as an opening rather t…” | “a bounded period of uncontended host conditions” | “the period during which agents are worktree-isola…” | “a bounded byte range read from the start of a ses…” | “a bounded period during which multiple sessions/r…” | “a time period tracked for account usage/quota acc…”
- **sweep** ×50: “a uniform mechanical pass applied across many fil…” | “a long uniform mechanical pass over many items” | “a review pass that clears accumulated venues or i…” | “the Architect's act of processing/clearing the in…” | “to clear and rule on every entry in an inbox in o…” | “a broad review-and-fix pass applying one law/stan…”
- **fire** ×49: “to launch a row's session from its work doc's own…” | “a kill criterion being triggered by the evidence” | “an agent being launched into flight” | “dispatch a board row's work doc as a live session” | “the named endpoint of the hands' wire contract th…” | “the act of launching a session via the glass/rig …”
- **cut** ×46: “to author a row into existence on the board” | “the act and product of composing a batch, as a no…” | “authoring a row, batch or entry into existence” | “create/spawn a new board row” | “to author/commit board rows, a batch, or a work d…” | “noun for the ritual sitting in which a new batch …”
- **arm** ×44: “one measured run or condition of an experiment” | “one branch of a probe — the control arm versus th…” | “enable a gated, potentially dangerous action once…” | “to authorize a declared flow to begin executing u…” | “to authorize a flow (or one of its steps) so the …” | “the cost/budget accounting performed at the momen…”
- **session** ×42: “one Claude conversation from boot to clear — the …” | “a single running Claude Code agent process/instan…” | “used loosely as a synonym for 'sitting' — the sam…” | “one of the desk's three named send-targets/routes…” | “a running instance of the engine/UI under test, d…” | “one bounded agent window that owes a ledger entry…”
- **pass** ×35: “a named check returning success” | “one sweep of human attention over an output” | “count of passing unit tests in a suite run” | “Felix's approval/proceed gesture on a waiting flo…” | “a review/inspection sweep of finished work (e.g. …” | “the flow engine's gesture of approving a paused F…”
- **sitting** ×35: “one Architect review session, treated as a recurr…” | “one continuous working session, counted as an occ…” | “the Architect's recurring review occasion” | “a recurring work session at which the Architect p…” | “a named, purpose-specific continuous work engagem…” | “a running agent session considered as a discrete …”
- **close** ×34: “a row reaching a terminal state — after which its…” | “the act of ending a session properly” | “tear down/release a cmux workspace opened by a fi…” | “the formal end-of-batch verification ritual (chec…” | “literal act of closing a cmux workspace as venue …” | “the ritual final dispatch marking the end of a ba…”
- **fence** ×31: “a code-fenced block holding text meant to be past…” | “the scope boundary inside which a Builder decides…” | “a mantle's write-authority boundary” | “a hard boundary/constraint established by a ratif…” | “the formal boundary defining which write actions …” | “minting site: the fence is amended to add new per…”
- **card** ×29: “a row as rendered in Belvedere's surface” | “a UI element on the rail/City view representing o…” | “a paused flow-engine node specifically requiring …” | “verb: to present an escalation to Felix as a card…” | “a rendered board-row element requiring Felix's ow…” | “to surface an item to Felix as a decision card in…”
- **register** ×28: “the doctrine-recognized/official record of live c…” | “the in-process cached snapshot of parsed city/boa…” | “the glass's cached in-memory parse of the whole c…” | “the glass's canonical directory-walking subsystem…” | “the doctrine rule set defining which directory co…” | “a cached/warmed lookup store (e.g. of accounts or…”
- **venue** ×27: “the live machine or environment work runs on” | “a live machine or container minted for a row's wo…” | “the environment/location in which a test is exerc…” | “the working environment (cmux workspace + scratch…” | “the recorded environmental footprint (branch, wor…” | “the document a table happens to live in”
- **land** ×23: “the board's own status text marking a step done, …” | “to arrive and come to rest in a file — of a repor…” | “to arrive and be filed in the inbox — not a row r…” | “a piece of vocabulary becoming finalized/ratified…” | “a document becoming final/adopted (here, before i…” | “a feature/functionality shipped and working, used…”
- **flow** ×23: “a JSON-defined automation pipeline of nodes (step…” | “a declared, executable work plan (a DAG of steps)…” | “an earlier, more conceptual notion of reactive/co…” | “a generic UX sense — a guided step-by-step wizard…” | “a JSON-defined pipeline of steps (a DAG) that the…” | “the DAG/pipeline object representing a sequence o…”
- **theater** ×23: “procedural apparatus performed for its own sake —…” | “the customizable name segment of a fired session'…” | “a naming-lineage exception for mantles (like the …” | “in the glass, the campaign name derived from the …” | “the working-directory-derived (or file-listed) ca…” | “a named working-directory/context slot assigned t…”
- **stamp** ×22: “the generated identifier label (mantle-theater-or…” | “the auto-generated session identifier (mantle-bui…” | “the original fired-session name-stamp, redefined …” | “a generated lineage identifier stamped onto a fir…” | “to formally apply/ratify a marker onto a decision” | “to mark a decision countersigned in the register”
- **wave** ×22: “a baton handing n instruments the holder could al…” | “a group of rows dispatched simultaneously within …” | “one of the three baton kinds (move / wave / fork)” | “a grouped set of ready-to-fire dispatches present…” | “verb: to let something through a checkpoint witho…” | “two or more sessions of the same mantle fired tog…”
- **hold** ×20: “generic sense: a ruling remains in force/valid” | “a board row's lifecycle word claiming that someon…” | “a row waiting, blocked pending another row's comp…” | “a document housing a section or record inside its…” | “to withhold a dispatch until a resource cools” | “for a dispatchable row to wait on another row's l…”
- **work** ×19: “the board column holding the row's one-line conte…” | “the totality of a building's past-and-planned eff…” | “any session's doing in the repo, as the thing the…” | “a scope class sizing a task against a session or …” | “work classified by which office owns the authorit…” | “a seat's output judged as an office contribution,…”
- **escalation** ×17: “an attention-class category: prose a Builder wrot…” | “a numbered board annotation (E<n>) naming an issu…” | “a noun-form instance: an issue flagged upward for…” | “a condition that must stop work and go up the cha…” | “a question a row refuses to answer itself and sen…” | “a filed item raising a question above the session…”
- **fork** ×16: “a decision point a running session can hit — must…” | “a baton whose move is Felix choosing between inst…” | “reality diverging from what the work doc anticipa…” | “a baton shape offering two exclusive next-step op…” | “an unresolved decision branch blocking a row unti…” | “a decision point a session will hit, to be pre-ru…”
- **kill criteria** ×16: “pre-written triggers that stop a line of work wit…” | “the pre-agreed conditions under which a row stops…” | “the conditions written in advance that end an exp…” | “pre-declared conditions under which an experiment…” | “a pre-registered stop condition for an experiment…” | “a pre-declared condition that aborts the session'…”
- **office** ×16: “a standing singular post, of which there is exact…” | “a Guild institutional authority that rules open v…” | “a canon-level governance body ruling on formats a…” | “replaces 'theater' as the working-identity slot a…” | “a continuing institution occupied by successive s…” | “a named post that exists independently of whoever…”
- **rig** ×15: “an assembled apparatus for running an experiment” | “a scratch apparatus (scripts) built to test a mec…” | “the pre-existing cmux/summon tooling layer Felix …” | “the keystroke launcher that fires mantled sessions” | “the summon.zsh tooling that composes and fires Cl…” | “the existing dispatch/summon tooling infrastructu…”
- **trap** ×14: “a rare item in a mechanical run that will be got …” | “a known hazard in a technology that justifies a h…” | “a hazard named in a work doc in advance, so its a…” | “an anticipated failure mode named in advance, to …” | “a documented hazard/gotcha the glass's design mus…” | “a measurement pitfall that produces a plausible-l…”
- **holder** ×14: “the single party whose move comes next after a se…” | “the structural rule determining which entity lega…” | “the baton field naming who currently owns a row's…” | “the structured-answer field naming who currently …” | “the role currently holding responsibility for an …” | “the single named party a handoff is addressed to”
- **census** ×13: “the append-only telemetry log of session/hook act…” | “Belvedere's own live account/session-gathering pr…” | “an exhaustive enumeration commissioned as its own…” | “a dated enumeration of a whole surface, taken so …” | “an enumeration of live sessions across the accoun…” | “the comprehensive collection of every term of art…”
- **gauge** ×13: “the named live measurement a Dispatcher reads bef…” | “the live measurement a Dispatcher watches before …” | “to assess a project incident for whether it carri…” | “the live meter of host or quota load that a Dispa…” | “the instrument that measures live load against th…” | “the resource metric (e.g. host load) that bounds …”
- **CLOSED** ×12: “a banned status token, superseded by LANDED or KI…” | “a batch-level completion status, distinct from th…” | “a campaign's DoD met — the same word D18 retired …” | “an open question settled by evidence and needing …” | “a whole campaign version finished against its DoD” | “a marker that an open unknown/question in a spike…”
- **probe** ×11: “an experiment run to detect whether an effect exi…” | “a diagnostic/investigative run producing findings” | “a Digger's numbered investigative work-doc (P-ser…” | “an empirical test run against real/live behavior …” | “a scripted experiment run to answer one factual q…” | “a prior fact-finding investigation whose conclusi…”
- **harness** ×11: “the Claude Code runtime that hosts sessions and e…” | “a browser-driven test apparatus proving a DoD cla…” | “the Claude Code runtime whose limits are physics …” | “the executable evidence rig a row's DoD is measur…” | “a third-party agent-running tool evaluated agains…” | “the automated test suite (lab/08/run) that exerci…”
- **spike** ×11: “a small bounded experimental project or investiga…” | “a disposable experiment run to confirm a mechanism” | “a bounded empirical probe run to settle a mechani…” | “a short exploratory experiment run to test a ques…” | “a bounded, disposable exploratory-build row genre…” | “an experimental, flag-gated code change built to …”
- **campaign** ×11: “a multi-session arc of work with a board behind it” | “a user-made group in the City arrangement, not a …” | “a bounded arc of rows with its own definition of …” | “a multi-session body of work with its own board, …” | “a multi-session bounded programme of work with it…” | “a bounded body of related planned sessions/rows u…”
- **ruling** ×10: “a decision Felix hands down that binds without fu…” | “a decision made on a live fork, weaker ceremony t…” | “the recorded decision an office makes when applyi…” | “Felix's personal decision act on phase acceptance…” | “Felix's phase-acceptance verdict, recorded as its…” | “a sequentially numbered, dated register of Felix'…”
- **landing** ×10: “the event of a row completing with its filing obl…” | “the recorded, completed work product of a board r…” | “the act of a board row reaching LANDED status, he…” | “the event of a row reaching LANDED, as a checkabl…” | “the event of a row's work arriving committed and …” | “the commit that fixes a document as unalterable r…”
- **closed** ×10: “a cmux workspace that has been shut, whose refere…” | “the completion state token for a batch or version…” | “a specific candidate option within a spec questio…” | “marking an escalation/open question as fully reso…” | “a batch of dispatched rows has finished, all land…” | “an open input-request/checklist item now fulfilled”
- **wedge** ×10: “an agent stuck and producing nothing” | “to hang or freeze a running shell/process, here t…” | “an instance/process stuck in a non-terminating st…” | “an instance of the simulator/harness hanging duri…” | “a hung/frozen browser-tab state caused by a block…” | “a hung browser-tab state, recurring term”
- **door** ×10: “the session boundary where handoff duties live” | “a small, deliberately narrow integration point le…” | “a narrow integration point deliberately left unbu…” | “a single narrow entry point through which all UI …” | “a single guarded entry point through which all ve…” | “the single sanctioned entry point/dispatch pipeli…”
- **re-cut** ×10: “to rewrite a row and its work doc after it blocked” | “to revise/reopen an already-cut work item, adding…” | “to reform an existing document into a new grammar…” | “to re-form existing documents into a new shape wi…” | “to redo/reformat an artifact (like a board's colu…” | “a wholesale re-authoring pass converting existing…”
- **kill criterion** ×9: “a pre-named condition whose firing justifies kill…” | “a probe's predefined failure threshold that, if t…” | “a pre-declared threshold that, if crossed, would …” | “a pre-declared evidentiary threshold that, if cro…” | “a pre-declared numeric or behavioral trigger that…” | “reuse instance confirming the kill-criterion term…”
- **rider** ×9: “a parenthetical annotation in a board cell, for e…” | “a mandatory attached condition on a permission — …” | “fixed appendix text auto-appended to every Belved…” | “a follow-up constraint or consideration from one …” | “a secondary consideration attached to a finding, …” | “a secondary issue noted alongside a finding, flag…”
- **instrument** ×9: “the concrete artifact that lets the holder act wi…” | “the payload/content object(s) a baton carries for…” | “the literal fire-now text in a baton — a pointer …” | “any item put to the sovereign for signature” | “a concrete named unit of action within a baton (e…” | “a concretely actionable directive in a baton's Ne…”
- **seam** ×9: “the interface by which pluggable tenants register…” | “a defined boundary/interface point in Belvedere's…” | “a deliberate injection point that lets a test sta…” | “a shared function/interface boundary through whic…” | “the point in a card's text marking where a short …” | “a deliberately exposed extension point in the cod…”
- **residue** ×9: “leftover disposable telemetry/artifacts from a Do…” | “what a pass deliberately leaves behind, kept visi…” | “the lint failures left after mechanical migration…” | “a field the automated migration cannot fill and m…” | “the parts of a migration a human must decide beca…” | “leftover artifacts/state left on a venue after a …”
- **wall** ×8: “the architectural invariant restricting native te…” | “reuse of the single-editable-row architectural in…” | “a hard constraint imposed by a specific D-entry r…” | “an environmental obstacle blocking a dispatched s…” | “a hard, named standing constraint from a prior de…” | “a named UI feature: a band of live screenshot pan…”
- **smoke** ×8: “a hands-on check Felix runs himself to confirm be…” | “the smoke test exercising the fixture city end-to…” | “Felix's own manual verification pass required bef…” | “a minimal live end-to-end trial, usually Felix's …” | “a manual end-to-end acceptance check performed pe…” | “a quick verification task type reserved for Felix…”
- **desk** ×8: “the office an item is routed to for judgment” | “the (not-yet-built) home for Felix's per-viewer w…” | “D17's built feature: a flat, frontmatter-free not…” | “idiom for the current sitting's (or Felix's) own …” | “the Architect's own holding place for rows betwee…” | “an office considered as the destination work is r…”
- **envelope** ×8: “the bounded scope of previously-approved design/s…” | “the bounded scope a spec/order is pre-authorized …” | “the scope boundary of a blessed batch — what work…” | “the scope boundary declared for a row, stating ex…” | “the declared scope boundary of authorized work fo…” | “the boundary of what a blessed batch already auth…”
- **shelf** ×8: “the accumulating list of one-line lessons each se…” | “the Grand Architect's Log shelf, cited as shared …” | “the UI listing of all resumable past sessions acr…” | “the glass feature listing and resuming any past s…” | “a v0 UI surface for session lists/resume, dissolv…” | “the page (`/shelf`) enumerating every session tra…”
- **cell** ×7: “one parallel agent slot within a dispatched batch” | “one named simulator VM/instance allocation dedica…” | “an isolated provisioned simulator instance-slot w…” | “a named instance of the simmy test harness enviro…” | “an auto-provisioned pool/execution environment th…” | “one lettered configuration slot (A, B, C...) in t…”
- **dod** ×7: “definition of done — the measurable completion co…” | “Definition of Done, the checklist/evidence bar a …” | “the definition-of-done checklist a row or campaig…” | “the Definition of Done — the enumerated, evidence…” | “the fixed acceptance-criteria section every plan/…” | “"Definition of Done" — the enumerated checklist a…”
- **STOP** ×7: “allcaps imperative marking a hard halt point in a…” | “a hard-halt outcome triggered when a kill criteri…” | “a hard-halt kill condition: if a named dangerous …” | “a scheduled deadline/cutoff time for a batch, aft…” | “an all-caps imperative marking an immediate halt-…” | “an imperative halt token in a kill/escalation cla…”
- **bench** ×7: “the office as a judicial seat with standing proce…” | “the physical robot test rig, as opposed to the si…” | “the venue-class of a real physical UR robot rig, …” | “a real-robot hardware verification session, as op…” | “the real-hardware test tier/session, as opposed t…” | “an automated performance-measurement harness (`?b…”
- **fold** ×7: “distilling evidence into the documents it changes” | “component name: a reader responsible for surfacin…” | “a queued unit of distillation work, named as a no…” | “the distilled conclusion a sitting produced” | “an app editor-verb name (bound to a key, collapsi…” | “moving ad hoc inline styling into the shared desi…”
- **glass** ×7: “Belvedere, the surface where boards are rendered …” | “Belvedere's scope stated as the sovereign's view …” | “literal greenhouse glass sheltering bred phenotyp…” | “literal greenhouse glass, second occurrence colli…” | “the touch/iOS coarse-pointer surface of the app, …” | “the touchscreen / touch-native surface of the iOS…”
- **building** ×7: “one repo or project, as a structure in the city” | “a repo/directory-level unit within the city that …” | “a project repo, in the city register, as a routin…” | “any directory carrying the Guild's record files —…” | “one repo/project in the city” | “a single project/repo, viewed as one structure wi…”
- **corpus** ×7: “the body of input material a test suite runs over” | “the discovered body of real doctrine artifacts a …” | “the body of canon/project documents that doctrine…” | “the body of Guild documents being parsed/analyzed…” | “the whole body of documents treated as one object…” | “the whole set of Guild documents a tool or experi…”
